---
phase: 05-anteprojetos-propostas-e-pre-os
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/components/platform/proposals/proposal-items-section.tsx
  - apps/web/components/platform/proposals/proposal-builder-page.tsx
autonomous: true
requirements:
  - PROP-02
user_setup: []
gap_closure: true
must_haves:
  truths:
    - "Price lookup selection writes itemCode, description, and unitPrice to the current form item row"
  artifacts:
    - path: "apps/web/components/platform/proposals/proposal-items-section.tsx"
      provides: "Price selection callback that writes to form fields"
      must_contain: "onPriceSelect prop calling setValue for itemCode, description, unitPrice"
    - path: "apps/web/components/platform/proposals/proposal-builder-page.tsx"
      provides: "Price selection handler passed to ProposalItemsSection"
      must_contain: "handlePriceSelect function calling setValue with items.${index} paths"
  key_links:
    - from: "apps/web/components/platform/proposals/proposal-builder-page.tsx"
      to: "ProposalItemsSection"
      via: "onPriceSelect prop"
      pattern: "onPriceSelect=\\{handlePriceSelect\\}"
    - from: "apps/web/components/platform/proposals/proposal-items-section.tsx"
      to: "form fields"
      via: "setValue callback from parent"
      pattern: "setValue\\(\\`items\\.\\$\\{activeItemIndex\\}\\.(itemCode|description|unitPrice)\\`\\)"
---

<objective>
Wire the price lookup dialog selection to write selected values back to the proposal form item row.

Purpose: Fix the partial gap where the price lookup dialog opens but does not persist selected itemCode, description, and unitPrice to the current form row. The user must be able to select a price table item and have those values populate the form fields automatically.
Output: Functional price lookup wiring with form field population.
</objective>

<execution_context>
@D:/Development/Fabersoft/hi-engenharia/code/.claude/get-shit-done/workflows/execute-plan.md
@D:/Development/Fabersoft/hi-engenharia/code/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-anteprojetos-propostas-e-pre-os/05-VERIFICATION.md
@.planning/phases/05-anteprojetos-propostas-e-pre-os/05-03-PLAN.md
@apps/web/components/platform/proposals/proposal-items-section.tsx
@apps/web/components/platform/proposals/proposal-builder-page.tsx
@apps/web/lib/proposal-form.ts

<interfaces>

From apps/web/lib/proposal-form.ts:
```typescript
export type ProposalFormValues = {
  title: string
  clientName: string
  projectDescription: string
  commercialTerms?: string
  validityDays: number
  items: Array<{
    id: string
    itemCode: string
    description: string
    quantity: number
    unitPrice: number
    totalPrice: number
  }>
}
```

From react-hook-form (UseFormSetValue):
```typescript
setValue<TFieldName extends string>(
  name: TFieldName,
  value: unknown,
  options?: SetValueOptions
): void
```

Form field paths for items array:
- `items.${index}.itemCode` - string
- `items.${index}.description` - string
- `items.${index}.unitPrice` - number
- `items.${index}.totalPrice` - number (derived from quantity * unitPrice)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lift price selection handler to ProposalBuilderPage and pass to ProposalItemsSection</name>
  <files>apps/web/components/platform/proposals/proposal-builder-page.tsx</files>
  <read_first>apps/web/components/platform/proposals/proposal-builder-page.tsx, apps/web/lib/proposal-form.ts, apps/web/components/platform/proposals/proposal-items-section.tsx</read_first>
  <action>In `apps/web/components/platform/proposals/proposal-builder-page.tsx`, add a new callback function `handlePriceSelect` that accepts four parameters: `itemIndex: number`, `itemCode: string`, `description: string`, `unitPrice: number`. Inside this function, call `setValue(`items.${itemIndex}.itemCode`, itemCode)`, `setValue(`items.${itemIndex}.description`, description)`, `setValue(`items.${itemIndex}.unitPrice`, unitPrice)`, and also calculate and set the total by calling `setValue(`items.${itemIndex}.totalPrice`, unitPrice * (watch(`items.${itemIndex}.quantity`) || 1))`. Pass this callback to `ProposalItemsSection` as a new prop named `onPriceSelect`. The prop signature must be: `onPriceSelect: (itemIndex: number, itemCode: string, description: string, unitPrice: number) => void`.</action>
  <verify>rg -n "handlePriceSelect|onPriceSelect|setValue\(\`items\.\\$\\{.*\\}\\.itemCode|setValue\(\`items\.\\$\\{.*\\}\\.description|setValue\(\`items\.\\$\\{.*\\}\\.unitPrice" apps/web/components/platform/proposals/proposal-builder-page.tsx</verify>
  <acceptance_criteria>
    - `apps/web/components/platform/proposals/proposal-builder-page.tsx` contains a function named `handlePriceSelect`
    - `apps/web/components/platform/proposals/proposal-builder-page.tsx` contains `setValue(\`items.${itemIndex}.itemCode\``
    - `apps/web/components/platform/proposals/proposal-builder-page.tsx` contains `setValue(\`items.${itemIndex}.description\``
    - `apps/web/components/platform/proposals/proposal-builder-page.tsx` contains `setValue(\`items.${itemIndex}.unitPrice\``
    - `apps/web/components/platform/proposals/proposal-builder-page.tsx` passes `onPriceSelect={handlePriceSelect}` to `ProposalItemsSection`
  </acceptance_criteria>
  <done>ProposalBuilderPage owns the price selection handler with access to setValue and passes it down to the items section.</done>
</task>

<task type="auto">
  <name>Task 2: Update ProposalItemsSection to use the onPriceSelect callback from parent</name>
  <files>apps/web/components/platform/proposals/proposal-items-section.tsx</files>
  <read_first>apps/web/components/platform/proposals/proposal-items-section.tsx, apps/web/components/platform/proposals/proposal-builder-page.tsx</read_first>
  <action>In `apps/web/components/platform/proposals/proposal-items-section.tsx`, add a new required prop to the interface: `onPriceSelect: (itemIndex: number, itemCode: string, description: string, unitPrice: number) => void`. Destructure this prop in the component. Update the `handlePriceSelect` function to call `onPriceSelect(activeItemIndex, itemCode, description, unitPrice)` instead of just closing the dialog, then close the dialog with `setPriceLookupOpen(false)` and reset `setActiveItemIndex(null)`. Remove the comment "// Note: This would need to be passed through props or context" as it is no longer applicable.</action>
  <verify>rg -n "onPriceSelect.*itemIndex.*itemCode.*description.*unitPrice|onPriceSelect\(activeItemIndex" apps/web/components/platform/proposals/proposal-items-section.tsx</verify>
  <acceptance_criteria>
    - `apps/web/components/platform/proposals/proposal-items-section.tsx` interface contains `onPriceSelect: (itemIndex: number, itemCode: string, description: string, unitPrice: number) => void`
    - `apps/web/components/platform/proposals/proposal-items-section.tsx` calls `onPriceSelect(activeItemIndex, itemCode, description, unitPrice)` inside `handlePriceSelect`
    - `apps/web/components/platform/proposals/proposal-items-section.tsx` does NOT contain the comment "// Note: This would need to be passed through props or context"
  </acceptance_criteria>
  <done>ProposalItemsSection uses the parent-provided callback to write selected price values to the form.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm --filter web lint && pnpm --filter web typecheck`
- [ ] `rg -n "handlePriceSelect|onPriceSelect" apps/web/components/platform/proposals/proposal-builder-page.tsx apps/web/components/platform/proposals/proposal-items-section.tsx`
- [ ] `rg -n "setValue\(\`items\." apps/web/components/platform/proposals/proposal-builder-page.tsx`
- [ ] `rg -n "onPriceSelect\(activeItemIndex" apps/web/components/platform/proposals/proposal-items-section.tsx`
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Price lookup selection writes itemCode, description, and unitPrice to the current form item row
- The gap identified in 05-VERIFICATION.md is closed
</success_criteria>

<output>
After completion, create `.planning/phases/05-anteprojetos-propostas-e-pre-os/05-05-SUMMARY.md`
</output>
