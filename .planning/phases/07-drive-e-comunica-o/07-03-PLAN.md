---
phase: 07-drive-e-comunica-o
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/lib/comunicacao-data.ts
  - apps/web/app/(platform)/comunicacao/page.tsx
  - apps/web/app/(platform)/comunicacao/[comunicadoId]/page.tsx
  - apps/web/components/platform/comunicacao/comunicacao-mural-page.tsx
  - apps/web/components/platform/comunicacao/comunicacao-card.tsx
  - apps/web/components/platform/comunicacao/comunicacao-toolbar.tsx
  - apps/web/components/platform/comunicacao/comunicacao-category-badge.tsx
  - apps/web/components/platform/comunicacao/comunicacao-detail-page.tsx
  - apps/web/components/platform/app-breadcrumbs.tsx
autonomous: true
requirements:
  - COMM-01
  - COMM-02
user_setup: []
must_haves:
  truths:
    - "Usuario pode visualizar um mural de comunicados com filtros por data e categoria"
    - "Comunicados com destaque aparecem primeiro com visual diferenciado (borda/fundo accent)"
    - "Cada card mostra titulo, resumo (2 linhas), categoria badge colorido, data, autor avatar + nome, e badge de destaque"
    - "Toolbar tem dropdown de categoria, seletor de periodo, busca e botao Novo comunicado"
    - "Usuario pode abrir o detalhe de um comunicado em pagina dedicada /comunicacao/[id]"
    - "Detalhe mostra conteudo completo, metadados, botoes Editar e Excluir"
    - "Excluir no detalhe mostra AlertDialog de confirmacao"
  artifacts:
    - path: "apps/web/lib/comunicacao-data.ts"
      provides: "Comunicacao data contract with types, mock data, category metadata, and helpers"
      exports: ["ComunicadoCategory", "Comunicado", "COMUNICADO_CATEGORY_META", "COMUNICADOS", "getComunicados", "getComunicadoById", "filterComunicados"]
    - path: "apps/web/components/platform/comunicacao/comunicacao-mural-page.tsx"
      provides: "Main mural feed page with card list, filters, and empty states"
    - path: "apps/web/components/platform/comunicacao/comunicacao-card.tsx"
      provides: "Single comunicado card with title, resumo, category badge, date, author, destaque"
    - path: "apps/web/components/platform/comunicacao/comunicacao-toolbar.tsx"
      provides: "Toolbar with category filter, period selector, search, and Novo comunicado CTA"
    - path: "apps/web/components/platform/comunicacao/comunicacao-category-badge.tsx"
      provides: "Color-coded category badge using Badge variants and custom classes"
    - path: "apps/web/components/platform/comunicacao/comunicacao-detail-page.tsx"
      provides: "Full comunicado detail page with content, metadata, edit/delete actions"
    - path: "apps/web/app/(platform)/comunicacao/[comunicadoId]/page.tsx"
      provides: "Dynamic route for comunicado detail"
  key_links:
    - "apps/web/app/(platform)/comunicacao/page.tsx imports ComunicacaoMuralPage instead of ModulePlaceholderPage"
    - "comunicacao-mural-page.tsx uses getComunicados and filterComunicados from comunicacao-data.ts"
    - "comunicacao-card.tsx renders ComunicadoCategoryBadge from comunicacao-category-badge.tsx"
    - "comunicacao-card.tsx is clickable and navigates to /comunicacao/[id] via next/link"
    - "apps/web/app/(platform)/comunicacao/[comunicadoId]/page.tsx resolves record via getComunicadoById and calls notFound() when missing"
    - "comunicacao-detail-page.tsx Excluir button opens AlertDialog confirmation"
    - "app-breadcrumbs.tsx handles /comunicacao/[id] routes showing Detalhe label"
---

<objective>
Criar o contrato de dados da Comunicacao, o mural de comunicados com filtros e cards, e a pagina de detalhe do comunicado.

Purpose: Tirar `/comunicacao` do placeholder, criar o mural com feed de cards, filtros, badges de categoria, e a pagina de detalhe com conteudo completo e acoes.
Output: Rota `/comunicacao` com mural filtrado, rota `/comunicacao/[comunicadoId]` com detalhe, contrato `comunicacao-data.ts`, badges de categoria, toolbar e empty states.
</objective>

<execution_context>
@D:/Development/Fabersoft/hi-engenharia/code/.claude/get-shit-done/workflows/execute-plan.md
@D:/Development/Fabersoft/hi-engenharia/code/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-drive-e-comunica-o/07-CONTEXT.md
@.planning/phases/07-drive-e-comunica-o/07-RESEARCH.md
@.planning/phases/07-drive-e-comunica-o/07-UI-SPEC.md
@apps/web/app/(platform)/comunicacao/page.tsx
@apps/web/lib/platform-config.ts
@apps/web/lib/budget-requests-data.ts
@apps/web/components/platform/module-placeholder-page.tsx
@apps/web/components/platform/app-breadcrumbs.tsx
@apps/web/components/platform/budget-requests/budget-requests-toolbar.tsx
@apps/web/components/platform/budget-requests/budget-requests-list-page.tsx
@apps/web/components/platform/crm/crm-opportunity-detail-page.tsx
@packages/ui/src/components/card.tsx
@packages/ui/src/components/badge.tsx
@packages/ui/src/components/avatar.tsx
@packages/ui/src/components/select.tsx
@packages/ui/src/components/input.tsx
@packages/ui/src/components/alert-dialog.tsx
@packages/ui/src/components/separator.tsx
@.claude/skills/shadcn/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the Comunicacao data contract and category badge component</name>
  <files>apps/web/lib/comunicacao-data.ts, apps/web/components/platform/comunicacao/comunicacao-category-badge.tsx</files>
  <read_first>apps/web/lib/budget-requests-data.ts, apps/web/lib/crm-data.ts, .planning/phases/07-drive-e-comunica-o/07-CONTEXT.md, .planning/phases/07-drive-e-comunica-o/07-RESEARCH.md, .planning/phases/07-drive-e-comunica-o/07-UI-SPEC.md, packages/ui/src/components/badge.tsx</read_first>
  <action>
**1. Create `apps/web/lib/comunicacao-data.ts`:**

**Types:**
- `export type ComunicadoCategory = "geral" | "operacional" | "comercial" | "rh" | "ti"`
- `export type ComunicadoPeriod = "all" | "today" | "week" | "month"`
- `export interface ComunicadoAuthor { id: string; name: string; initials: string }`
- `export interface Comunicado { id: string; title: string; summary: string; content: string; category: ComunicadoCategory; author: ComunicadoAuthor; publishedAt: string; isDestaque: boolean; isDraft: boolean }`

**Category metadata constant:**
```typescript
export const COMUNICADO_CATEGORY_META: Record<
  ComunicadoCategory,
  { label: string; variant: "default" | "secondary" | "outline" | "destructive"; className?: string }
> = {
  geral: { label: "Geral", variant: "secondary" },
  operacional: { label: "Operacional", variant: "default" },
  comercial: { label: "Comercial", variant: "outline" },
  rh: { label: "RH", variant: "secondary", className: "bg-amber-100 text-amber-800 dark:bg-amber-900 dark:text-amber-200" },
  ti: { label: "TI", variant: "secondary", className: "bg-blue-100 text-blue-800 dark:bg-blue-900 dark:text-blue-200" },
}
```

**Mock data:**
- `export const COMUNICADO_AUTHORS: ComunicadoAuthor[]` -- at least 3 authors
- `export const COMUNICADOS: Comunicado[]` -- at least 8 comunicados. At least 2 must have `isDestaque: true`. Mix all 5 categories. Use realistic Portuguese titles and summaries (2-3 sentences). Content should be 2-3 paragraphs of Portuguese text. All `isDraft: false`. Dates spread across the last 30 days in ISO format.

**Helpers:**
- `export function getComunicados(): Comunicado[]` -- returns all non-draft comunicados sorted: destaques first, then by date descending
- `export function getComunicadoById(id: string): Comunicado | undefined`
- `export function getComunicadoCategoryOptions(): Array<{ value: string; label: string }>` -- returns `[{ value: "all", label: "Todas as categorias" }, ...categories]`
- `export function getComunicadoPeriodOptions(): Array<{ value: ComunicadoPeriod; label: string }>` -- returns options: `Qualquer periodo`, `Hoje`, `Ultima semana`, `Ultimo mes`
- `export function filterComunicados(params: { category: string; period: ComunicadoPeriod; searchQuery: string }): Comunicado[]` -- filters by category (if not "all"), by period (filtering by `publishedAt` date), and by search query (matching title or summary case-insensitive). Always sort destaques first, then by date descending.

**2. Create `apps/web/components/platform/comunicacao/comunicacao-category-badge.tsx`:**
A `"use client"` component that accepts `category: ComunicadoCategory`. Render a `Badge` from `@workspace/ui/components/badge`. Use `COMUNICADO_CATEGORY_META` to resolve the variant. For categories with a `className` property (rh, ti), apply that className via `cn()` overriding the variant background. For other categories, just use the variant prop. Display the label from the metadata.
  </action>
  <verify>
    <automated>cd D:/Development/Fabersoft/hi-engenharia/code && npx rg -n "ComunicadoCategory|Comunicado |COMUNICADO_CATEGORY_META|COMUNICADOS|getComunicados|getComunicadoById|getComunicadoCategoryOptions|getComunicadoPeriodOptions|filterComunicados|isDestaque|isDraft" apps/web/lib/comunicacao-data.ts && npx rg -n "Badge|COMUNICADO_CATEGORY_META|ComunicadoCategory|bg-amber-100|bg-blue-100" apps/web/components/platform/comunicacao/comunicacao-category-badge.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `comunicacao-data.ts` contains `export type ComunicadoCategory = "geral" | "operacional" | "comercial" | "rh" | "ti"`
    - `comunicacao-data.ts` contains `export interface Comunicado`
    - `comunicacao-data.ts` contains `export const COMUNICADO_CATEGORY_META`
    - `comunicacao-data.ts` contains `export const COMUNICADOS`
    - `comunicacao-data.ts` contains `export function getComunicados`
    - `comunicacao-data.ts` contains `export function getComunicadoById`
    - `comunicacao-data.ts` contains `export function filterComunicados`
    - `comunicacao-data.ts` contains `export function getComunicadoCategoryOptions`
    - `comunicacao-data.ts` contains `export function getComunicadoPeriodOptions`
    - `comunicacao-data.ts` contains `Todas as categorias` and `Qualquer periodo`
    - `comunicacao-data.ts` has `isDestaque` and `isDraft` fields
    - `comunicacao-data.ts` contains `bg-amber-100 text-amber-800` for RH category
    - `comunicacao-data.ts` contains `bg-blue-100 text-blue-800` for TI category
    - `comunicacao-category-badge.tsx` imports `Badge` from `@workspace/ui/components/badge`
    - `comunicacao-category-badge.tsx` imports `COMUNICADO_CATEGORY_META`
  </acceptance_criteria>
  <done>The Comunicacao module has a complete data contract with typed comunicados, category metadata with color mappings, mock data with destaques, and a reusable category badge component.</done>
</task>

<task type="auto">
  <name>Task 2: Build the mural feed page with card list, toolbar, and empty states</name>
  <files>apps/web/components/platform/comunicacao/comunicacao-card.tsx, apps/web/components/platform/comunicacao/comunicacao-toolbar.tsx, apps/web/components/platform/comunicacao/comunicacao-mural-page.tsx, apps/web/app/(platform)/comunicacao/page.tsx</files>
  <read_first>apps/web/lib/comunicacao-data.ts, apps/web/components/platform/comunicacao/comunicacao-category-badge.tsx, apps/web/components/platform/budget-requests/budget-requests-toolbar.tsx, apps/web/components/platform/budget-requests/budget-requests-list-page.tsx, .planning/phases/07-drive-e-comunica-o/07-CONTEXT.md, .planning/phases/07-drive-e-comunica-o/07-UI-SPEC.md, apps/web/app/(platform)/comunicacao/page.tsx, packages/ui/src/components/card.tsx, packages/ui/src/components/avatar.tsx, packages/ui/src/components/badge.tsx, packages/ui/src/components/select.tsx, packages/ui/src/components/input.tsx</read_first>
  <action>
**1. Create `apps/web/components/platform/comunicacao/comunicacao-card.tsx`:**
A `"use client"` component that accepts `comunicado: Comunicado`. Render a `Card` wrapped in a `next/link` to `/comunicacao/${comunicado.id}`. The entire card is clickable.

Card content layout:
- `CardHeader`: Row with `ComunicadoCategoryBadge` and, if `isDestaque`, an additional `Badge` with text `Destaque` using variant `default`.
- `CardContent`:
  - Title in `text-lg font-semibold` (heading weight)
  - Summary text with `line-clamp-2 text-muted-foreground` for 2-line truncation
- `CardFooter`: Row with author (Avatar with AvatarFallback showing initials + name text) and date formatted as DD/MM/AAAA using `new Date(comunicado.publishedAt).toLocaleDateString("pt-BR")`.

Destaque visual: When `comunicado.isDestaque` is true, add `border-l-4 border-l-primary bg-primary/5` to the Card's className to create the accent left border and subtle background emphasis.

The card must have `hover:shadow-md transition-shadow` for interactive feedback.

**2. Create `apps/web/components/platform/comunicacao/comunicacao-toolbar.tsx`:**
A `"use client"` component that accepts `categoryFilter`, `onCategoryFilterChange`, `periodFilter`, `onPeriodFilterChange`, `searchQuery`, `onSearchQueryChange`, and `onNewComunicado`. Render:
- `Select` for category with options from `getComunicadoCategoryOptions()`. Default placeholder `Todas as categorias`.
- `Select` for period with options from `getComunicadoPeriodOptions()`. Default placeholder `Qualquer periodo`.
- Search input with `SearchIcon` and placeholder `Buscar comunicados`.
- Button with text `Novo comunicado` and `PlusIcon`, variant default (primary).

Layout: `flex flex-col gap-3 md:flex-row md:items-center md:gap-4`. On mobile, the `Novo comunicado` button should be at the top (use `order-first md:order-last`).

**3. Create `apps/web/components/platform/comunicacao/comunicacao-mural-page.tsx`:**
A `"use client"` component that manages filter state:
- `categoryFilter = "all"` (default)
- `periodFilter: ComunicadoPeriod = "all"` (default)
- `searchQuery = ""` (default)

Calls `filterComunicados({ category: categoryFilter, period: periodFilter, searchQuery })` to derive the displayed comunicados. Renders `ComunicacaoToolbar` at the top, then a vertical card feed list with `ComunicacaoCard` for each comunicado. Use `flex flex-col gap-4` for card spacing.

Empty states:
- When there are no comunicados at all (fresh state): heading `Nenhum comunicado publicado` with body `Publique o primeiro comunicado para informar a equipe.` and a Button `Publicar`.
- When filters return no results: heading `Nenhum comunicado encontrado` with body `Ajuste os filtros ou termos de busca.`

The `onNewComunicado` callback from the toolbar should set a `publishDialogOpen` state to true (the actual dialog will be wired in Plan 04, for now just store the state).

**4. Overwrite `apps/web/app/(platform)/comunicacao/page.tsx`:**
Replace the placeholder with:
```typescript
import { ComunicacaoMuralPage } from "@/components/platform/comunicacao/comunicacao-mural-page"

export default function ComunicacaoRoute() {
  return <ComunicacaoMuralPage />
}
```
Remove all `ModulePlaceholderPage` imports and usage.
  </action>
  <verify>
    <automated>cd D:/Development/Fabersoft/hi-engenharia/code && npx rg -n "ComunicacaoMuralPage" "apps/web/app/(platform)/comunicacao/page.tsx" && npx rg -n "ModulePlaceholderPage" "apps/web/app/(platform)/comunicacao/page.tsx" ; npx rg -n "Comunicado|ComunicadoCategoryBadge|line-clamp-2|isDestaque|border-l-primary|bg-primary/5|Destaque|AvatarFallback|toLocaleDateString" apps/web/components/platform/comunicacao/comunicacao-card.tsx && npx rg -n "Todas as categorias|Qualquer periodo|Buscar comunicados|Novo comunicado|SearchIcon|PlusIcon" apps/web/components/platform/comunicacao/comunicacao-toolbar.tsx && npx rg -n "filterComunicados|categoryFilter|periodFilter|searchQuery|Nenhum comunicado publicado|Nenhum comunicado encontrado|Publique o primeiro|Ajuste os filtros|publishDialogOpen" apps/web/components/platform/comunicacao/comunicacao-mural-page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/app/(platform)/comunicacao/page.tsx` contains `ComunicacaoMuralPage` and does NOT contain `ModulePlaceholderPage`
    - `comunicacao-card.tsx` contains `ComunicadoCategoryBadge`
    - `comunicacao-card.tsx` contains `line-clamp-2`
    - `comunicacao-card.tsx` contains `border-l-primary` and `bg-primary/5` for destaque styling
    - `comunicacao-card.tsx` contains `Destaque` badge text
    - `comunicacao-card.tsx` contains `AvatarFallback`
    - `comunicacao-card.tsx` contains `toLocaleDateString("pt-BR")`
    - `comunicacao-toolbar.tsx` contains exact string `Todas as categorias`
    - `comunicacao-toolbar.tsx` contains exact string `Qualquer periodo`
    - `comunicacao-toolbar.tsx` contains exact string `Buscar comunicados`
    - `comunicacao-toolbar.tsx` contains exact string `Novo comunicado`
    - `comunicacao-mural-page.tsx` contains `filterComunicados`
    - `comunicacao-mural-page.tsx` contains exact string `Nenhum comunicado publicado`
    - `comunicacao-mural-page.tsx` contains exact string `Publique o primeiro comunicado para informar a equipe.`
    - `comunicacao-mural-page.tsx` contains exact string `Nenhum comunicado encontrado`
    - `comunicacao-mural-page.tsx` contains exact string `Ajuste os filtros ou termos de busca.`
    - `comunicacao-mural-page.tsx` contains `publishDialogOpen`
  </acceptance_criteria>
  <done>The Comunicacao mural shows a filtered card feed with category badges, destaque emphasis, search, and proper empty states. The placeholder page is fully replaced.</done>
</task>

<task type="auto">
  <name>Task 3: Build the comunicado detail page with dynamic route and breadcrumb support</name>
  <files>apps/web/app/(platform)/comunicacao/[comunicadoId]/page.tsx, apps/web/components/platform/comunicacao/comunicacao-detail-page.tsx, apps/web/components/platform/app-breadcrumbs.tsx</files>
  <read_first>apps/web/lib/comunicacao-data.ts, apps/web/components/platform/comunicacao/comunicacao-category-badge.tsx, apps/web/app/(platform)/orcamentos/[requestId]/page.tsx, apps/web/components/platform/crm/crm-opportunity-detail-page.tsx, apps/web/components/platform/app-breadcrumbs.tsx, .planning/phases/07-drive-e-comunica-o/07-CONTEXT.md, .planning/phases/07-drive-e-comunica-o/07-UI-SPEC.md, packages/ui/src/components/alert-dialog.tsx, packages/ui/src/components/separator.tsx</read_first>
  <action>
**1. Create `apps/web/app/(platform)/comunicacao/[comunicadoId]/page.tsx`:**
A server component using the Next.js 16 pattern with `params: Promise<{ comunicadoId: string }>`. Await params, resolve `comunicadoId`, call `getComunicadoById(comunicadoId)`. If not found, call `notFound()` from `next/navigation`. Render `ComunicacaoDetailPage` passing the resolved comunicado.

**2. Create `apps/web/components/platform/comunicacao/comunicacao-detail-page.tsx`:**
A `"use client"` component that accepts `comunicado: Comunicado`. Render a detail page with:

Layout: Single column, max-width `max-w-3xl mx-auto`.

Header section:
- Back link: `Voltar` with ArrowLeftIcon, navigates to `/comunicacao` via `next/link`.
- Title in `text-2xl font-semibold` (display size).
- Metadata row: `ComunicadoCategoryBadge`, date formatted DD/MM/AAAA, and if `isDestaque`, a `Badge` with text `Destaque`.
- Author row: Avatar with AvatarFallback + author name.

Content section:
- `Separator` below header.
- Full content text rendered in `prose` or `text-base leading-relaxed whitespace-pre-wrap` for paragraph preservation.

Actions section:
- `Separator` above actions.
- Two buttons in a row with `gap-3`:
  - `Editar` button with PencilIcon, variant outline. Navigates to `/comunicacao/${comunicado.id}/editar` via `next/link` (the edit page will be created in Plan 04).
  - `Excluir` button with Trash2Icon, variant destructive outline.

Delete flow: The `Excluir` button opens an `AlertDialog` with:
- Title: `Excluir comunicado`
- Description: `Tem certeza que deseja excluir este comunicado? Esta acao nao pode ser desfeita.`
- Cancel button: `Cancelar`
- Confirm button: `Excluir` in destructive variant
- On confirm: show `toast.success("Comunicado excluido")` and navigate to `/comunicacao` via `useRouter().push`.

**3. Update `apps/web/components/platform/app-breadcrumbs.tsx`:**
Add breadcrumb handling for `/comunicacao/[id]` routes. When pathname matches `/comunicacao/{dynamic-id}` (a segment after `/comunicacao/`), render the breadcrumb as: `Comunicacao > Detalhe`. Also handle `/comunicacao/{id}/editar` as `Comunicacao > Detalhe > Editar` for the edit page (to be created in Plan 04). Follow the exact same pattern used for `/anteprojetos/[id]` and `/crm/[id]` breadcrumb handling already in the file.
  </action>
  <verify>
    <automated>cd D:/Development/Fabersoft/hi-engenharia/code && npx rg -n "params: Promise|getComunicadoById|notFound|ComunicacaoDetailPage" "apps/web/app/(platform)/comunicacao/[comunicadoId]/page.tsx" && npx rg -n "Voltar|ComunicadoCategoryBadge|toLocaleDateString|Editar|Excluir|AlertDialog|Excluir comunicado|Esta acao nao pode ser desfeita|Cancelar|toast.success|useRouter" apps/web/components/platform/comunicacao/comunicacao-detail-page.tsx && npx rg -n "comunicacao|Detalhe|Editar" apps/web/components/platform/app-breadcrumbs.tsx && pnpm build --force</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/app/(platform)/comunicacao/[comunicadoId]/page.tsx` contains `params: Promise<{ comunicadoId: string }>`
    - `apps/web/app/(platform)/comunicacao/[comunicadoId]/page.tsx` contains `getComunicadoById`
    - `apps/web/app/(platform)/comunicacao/[comunicadoId]/page.tsx` contains `notFound`
    - `comunicacao-detail-page.tsx` contains `Voltar` link
    - `comunicacao-detail-page.tsx` contains `ComunicadoCategoryBadge`
    - `comunicacao-detail-page.tsx` contains `toLocaleDateString("pt-BR")`
    - `comunicacao-detail-page.tsx` contains `Editar` button
    - `comunicacao-detail-page.tsx` contains `Excluir` button
    - `comunicacao-detail-page.tsx` contains `AlertDialog` or `AlertDialogContent`
    - `comunicacao-detail-page.tsx` contains exact string `Excluir comunicado`
    - `comunicacao-detail-page.tsx` contains exact string `Tem certeza que deseja excluir este comunicado? Esta acao nao pode ser desfeita.`
    - `comunicacao-detail-page.tsx` contains `toast` import (from sonner)
    - `comunicacao-detail-page.tsx` contains `useRouter`
    - `app-breadcrumbs.tsx` contains `comunicacao` handling
    - `app-breadcrumbs.tsx` contains `Detalhe` label for comunicacao routes
    - `pnpm build --force` exits 0
  </acceptance_criteria>
  <done>The Comunicacao module has a full detail page at /comunicacao/[id] with content, metadata, category badge, edit/delete actions, delete confirmation dialog, and proper breadcrumb navigation.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm build --force`
- [ ] `pnpm --filter web lint`
- [ ] `npx rg -n "ComunicacaoMuralPage" "apps/web/app/(platform)/comunicacao/page.tsx"`
- [ ] `npx rg -n "ModulePlaceholderPage" "apps/web/app/(platform)/comunicacao/page.tsx"` returns NO matches
- [ ] `npx rg -n "COMUNICADO_CATEGORY_META|getComunicados|filterComunicados" apps/web/lib/comunicacao-data.ts`
- [ ] `npx rg -n "Nenhum comunicado publicado|Nenhum comunicado encontrado" apps/web/components/platform/comunicacao/comunicacao-mural-page.tsx`
- [ ] `npx rg -n "Excluir comunicado|Esta acao nao pode ser desfeita" apps/web/components/platform/comunicacao/comunicacao-detail-page.tsx`
- [ ] `npx rg -n "comunicacao.*Detalhe" apps/web/components/platform/app-breadcrumbs.tsx`
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No build errors or lint warnings introduced
- `/comunicacao` renders a card feed with filtered comunicados sorted by destaque then date
- Category badges display with correct colors per category
- Destaque cards have visual accent emphasis (border-left, subtle background)
- Toolbar filters work for category, period, and search
- Empty states render correctly
- `/comunicacao/[id]` shows full content with metadata and action buttons
- Delete confirmation dialog works with toast feedback and redirect
- Breadcrumbs show correct labels for detail routes
</success_criteria>

<output>
After completion, create `.planning/phases/07-drive-e-comunica-o/07-03-SUMMARY.md`
</output>
