---
phase: 04-crm-e-pipeline-comercial
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - apps/web/app/(platform)/crm/[opportunityId]/page.tsx
  - apps/web/components/platform/app-breadcrumbs.tsx
  - apps/web/components/platform/crm/crm-opportunity-detail-page.tsx
  - apps/web/components/platform/crm/crm-stage-history.tsx
  - apps/web/components/platform/crm/crm-stage-change-select.tsx
autonomous: true
requirements:
  - CRM-02
user_setup: []
must_haves:
  truths:
    - `/crm/[opportunityId]` renders a real opportunity detail page from the seeded CRM data contract and calls `notFound()` for missing IDs.
    - The detail screen shows the locked two-column layout with current stage, history, responsible people, and the origin orçamento link.
    - Changing stage in the detail view updates local state and appends a new history entry without introducing persistence or backend logic.
  artifacts:
    - apps/web/app/(platform)/crm/[opportunityId]/page.tsx
    - apps/web/components/platform/app-breadcrumbs.tsx
    - apps/web/components/platform/crm/crm-opportunity-detail-page.tsx
    - apps/web/components/platform/crm/crm-stage-history.tsx
    - apps/web/components/platform/crm/crm-stage-change-select.tsx
  key_links:
    - `apps/web/app/(platform)/crm/[opportunityId]/page.tsx` resolves a record through `getCrmOpportunityById(opportunityId)` and calls `notFound()` when missing.
    - `apps/web/components/platform/app-breadcrumbs.tsx` maps nested CRM detail paths to the exact breadcrumb label `Detalhe`.
    - `apps/web/components/platform/crm/crm-opportunity-detail-page.tsx` renders both `CrmStageHistory` and `CrmStageChangeSelect`.
---

<objective>
Entregar a tela de detalhe do negocio com leitura clara de status, historico e conexao visual com o fluxo de orcamentos.

Purpose: Transformar o CRM em uma experiencia navegavel de negocio individual, com breadcrumb coerente, dados mockados completos e mudanca local de etapa.
Output: Rota dinamica `/crm/[opportunityId]`, breadcrumb de detalhe, layout final de negocio, timeline visual e seletor de etapa.
</objective>

<execution_context>
@D:/Development/Fabersoft/hi-engenharia/code/.codex/get-shit-done/workflows/execute-plan.md
@D:/Development/Fabersoft/hi-engenharia/code/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-crm-e-pipeline-comercial/04-CONTEXT.md
@.planning/phases/04-crm-e-pipeline-comercial/04-RESEARCH.md
@.planning/phases/04-crm-e-pipeline-comercial/04-VALIDATION.md
@.planning/phases/04-crm-e-pipeline-comercial/04-UI-SPEC.md
@.planning/phases/04-crm-e-pipeline-comercial/04-01-PLAN.md
@apps/web/lib/crm-data.ts
@apps/web/components/platform/app-breadcrumbs.tsx
@apps/web/app/(platform)/orcamentos/[requestId]/page.tsx
@apps/web/components/platform/budget-requests/budget-request-detail-page.tsx
@packages/ui/src/components/card.tsx
@packages/ui/src/components/select.tsx
@packages/ui/src/components/badge.tsx
@packages/ui/src/components/button.tsx
@packages/ui/src/components/separator.tsx
@.agents/skills/shadcn/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the CRM detail route boundary and nested breadcrumb labeling</name>
  <files>apps/web/app/(platform)/crm/[opportunityId]/page.tsx, apps/web/components/platform/app-breadcrumbs.tsx</files>
  <read_first>apps/web/app/(platform)/orcamentos/[requestId]/page.tsx, apps/web/components/platform/app-breadcrumbs.tsx, apps/web/lib/crm-data.ts, .planning/phases/04-crm-e-pipeline-comercial/04-RESEARCH.md, .planning/phases/04-crm-e-pipeline-comercial/04-UI-SPEC.md</read_first>
  <action>Create `apps/web/app/(platform)/crm/[opportunityId]/page.tsx` using the exact Next.js 16 signature `params: Promise<{ opportunityId: string }>`; resolve `opportunityId`, load the record through `getCrmOpportunityById(opportunityId)`, and call `notFound()` when the record does not exist. Update `apps/web/components/platform/app-breadcrumbs.tsx` so any pathname matching `/crm/{dynamic-id}` renders the second breadcrumb item as the exact label `Detalhe` while keeping the first item `CRM`. Do not hardcode specific opportunity IDs into the breadcrumb logic.</action>
  <verify>rg -n "params: Promise<\\{ opportunityId: string \\}>|getCrmOpportunityById|notFound" "apps/web/app/(platform)/crm/[opportunityId]/page.tsx" && rg -n "Detalhe|pathname|/crm/" apps/web/components/platform/app-breadcrumbs.tsx</verify>
  <acceptance_criteria>
    - `apps/web/app/(platform)/crm/[opportunityId]/page.tsx` contains `params: Promise<{ opportunityId: string }>`
    - `apps/web/app/(platform)/crm/[opportunityId]/page.tsx` contains `getCrmOpportunityById`
    - `apps/web/app/(platform)/crm/[opportunityId]/page.tsx` contains `notFound`
    - `apps/web/components/platform/app-breadcrumbs.tsx` contains the exact label `Detalhe`
    - `apps/web/components/platform/app-breadcrumbs.tsx` contains pathname matching logic for `/crm/`
  </acceptance_criteria>
  <done>The CRM detail route has a stable App Router boundary and the shell can identify nested CRM pages correctly.</done>
</task>

<task type="auto">
  <name>Task 2: Build the opportunity detail layout and stage-history presentation</name>
  <files>apps/web/components/platform/crm/crm-opportunity-detail-page.tsx, apps/web/components/platform/crm/crm-stage-history.tsx</files>
  <read_first>apps/web/components/platform/budget-requests/budget-request-detail-page.tsx, apps/web/lib/crm-data.ts, .planning/phases/04-crm-e-pipeline-comercial/04-CONTEXT.md, .planning/phases/04-crm-e-pipeline-comercial/04-UI-SPEC.md, packages/ui/src/components/card.tsx, packages/ui/src/components/badge.tsx, packages/ui/src/components/separator.tsx</read_first>
  <action>Create `apps/web/components/platform/crm/crm-stage-history.tsx` so it renders history rows from `CrmHistoryEntry` with stage label, `changedAt`, and responsible name. Create `crm-opportunity-detail-page.tsx` with the exact layout class string `lg:grid lg:grid-cols-[minmax(0,1.2fr)_minmax(320px,0.8fr)] lg:gap-6`. The left column must render the exact labels `Empresa/cliente`, `Contato`, `Valor estimado`, and `Responsaveis`. The right column must render the exact headings `Etapa atual`, `Historico visual`, and `Acoes`. Render `Solicitacao de origem` as a dedicated block linked to `/orcamentos/{originBudgetRequestId}` using the CRM record field, and use responsive ordering so it appears with the summary content on desktop but after the stage-change action on mobile. Reuse `CrmStageBadge` and `CrmPriorityBadge` rather than hardcoded badge markup.</action>
  <verify>rg -n "changedAt|Responsaveis|Solicitacao de origem|Etapa atual|Historico visual|Acoes|/orcamentos/|lg:grid lg:grid-cols-\\[minmax\\(0,1.2fr\\)_minmax\\(320px,0.8fr\\)\\]" apps/web/components/platform/crm/crm-opportunity-detail-page.tsx apps/web/components/platform/crm/crm-stage-history.tsx</verify>
  <acceptance_criteria>
    - `apps/web/components/platform/crm/crm-opportunity-detail-page.tsx` contains the exact labels `Empresa/cliente`, `Contato`, `Valor estimado`, `Responsaveis`, and `Solicitacao de origem`
    - `apps/web/components/platform/crm/crm-opportunity-detail-page.tsx` contains the exact headings `Etapa atual`, `Historico visual`, and `Acoes`
    - `apps/web/components/platform/crm/crm-opportunity-detail-page.tsx` contains the exact class string `lg:grid lg:grid-cols-[minmax(0,1.2fr)_minmax(320px,0.8fr)] lg:gap-6`
    - `apps/web/components/platform/crm/crm-opportunity-detail-page.tsx` contains responsive ordering for `Solicitacao de origem`
    - `apps/web/components/platform/crm/crm-opportunity-detail-page.tsx` contains `/orcamentos/`
    - `apps/web/components/platform/crm/crm-stage-history.tsx` contains `changedAt`
  </acceptance_criteria>
  <done>The detail screen communicates business context, current stage, and origin handoff in the locked desktop/mobile structure.</done>
</task>

<task type="auto">
  <name>Task 3: Implement local stage changes and history append behavior</name>
  <files>apps/web/components/platform/crm/crm-opportunity-detail-page.tsx, apps/web/components/platform/crm/crm-stage-change-select.tsx</files>
  <read_first>apps/web/components/platform/crm/crm-opportunity-detail-page.tsx, apps/web/lib/crm-data.ts, .planning/phases/04-crm-e-pipeline-comercial/04-CONTEXT.md, .planning/phases/04-crm-e-pipeline-comercial/04-UI-SPEC.md, packages/ui/src/components/select.tsx, packages/ui/src/components/button.tsx</read_first>
  <action>Create `apps/web/components/platform/crm/crm-stage-change-select.tsx` with a shadcn `Select` that reads the exact options from `CRM_STAGE_ORDER` and uses the exact visible label `Mudar etapa`. Update `CrmOpportunityDetailPage` so it owns local opportunity state, passes the current stage into `CrmStageChangeSelect`, and on selection writes these exact changes: set `stage` to the selected stage id, prepend one history entry object with the keys `stage`, `changedAt`, and `changedBy`, set `changedAt` to `new Date().toISOString()`, and set `changedBy` to the current responsible person's name. Do not mutate the shared `CRM_OPPORTUNITIES` export and do not add persistence, server actions, or URL state.</action>
  <verify>rg -n "Mudar etapa|CRM_STAGE_ORDER|Select" apps/web/components/platform/crm/crm-stage-change-select.tsx && rg -n "new Date\\(\\)\\.toISOString\\(\\)|changedAt|changedBy|setOpportunity|Mudar etapa" apps/web/components/platform/crm/crm-opportunity-detail-page.tsx</verify>
  <acceptance_criteria>
    - `apps/web/components/platform/crm/crm-stage-change-select.tsx` contains the exact label `Mudar etapa`
    - `apps/web/components/platform/crm/crm-stage-change-select.tsx` contains `CRM_STAGE_ORDER`
    - `apps/web/components/platform/crm/crm-opportunity-detail-page.tsx` contains `new Date().toISOString()`
    - `apps/web/components/platform/crm/crm-opportunity-detail-page.tsx` contains the exact keys `changedAt` and `changedBy`
    - `apps/web/components/platform/crm/crm-opportunity-detail-page.tsx` contains `setOpportunity`
  </acceptance_criteria>
  <done>The opportunity detail view now supports a believable mock stage-change flow with immediate visual history feedback.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm lint && pnpm typecheck && pnpm build`
- [ ] `rg -n "getCrmOpportunityById|notFound" "apps/web/app/(platform)/crm/[opportunityId]/page.tsx"`
- [ ] `rg -n "Detalhe" apps/web/components/platform/app-breadcrumbs.tsx`
- [ ] `rg -n "Mudar etapa|Solicitacao de origem|Historico visual" apps/web/components/platform/crm/crm-opportunity-detail-page.tsx`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The CRM flow has a real opportunity detail page with local stage changes, history, and a visible link back to orçamento
  </success_criteria>

<output>
After completion, create `.planning/phases/04-crm-e-pipeline-comercial/04-02-SUMMARY.md`
</output>
