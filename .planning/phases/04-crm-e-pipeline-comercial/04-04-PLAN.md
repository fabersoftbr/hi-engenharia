---
phase: 04-crm-e-pipeline-comercial
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/components/platform/crm/crm-workspace-page.tsx
  - apps/web/components/platform/crm/crm-pipeline-card.tsx
autonomous: true
gap_closure: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Toggle buttons and filters visible without horizontal scroll on /crm page"
    - "Card shows responsible person full name alongside avatar initials"
  artifacts:
    - path: "apps/web/components/platform/crm/crm-workspace-page.tsx"
      provides: "CRM workspace header with responsive layout"
      contains: "md:flex-row"
    - path: "apps/web/components/platform/crm/crm-pipeline-card.tsx"
      provides: "Kanban card with owner name display"
      contains: "owner?.name"
  key_links:
    - from: "crm-workspace-page.tsx header"
      to: "viewport 640-900px"
      via: "flex-wrap/md breakpoint"
      pattern: "md:flex-row"
    - from: "CrmPipelineCard"
      to: "owner data"
      via: "text element after Avatar"
      pattern: "owner\\?\\.name"
---

<objective>
Fix 2 UAT-diagnosed gaps in CRM module: header scroll overflow on medium viewports and missing responsible name on Kanban cards.

Purpose: Restore expected UX where header controls wrap properly and card shows who is responsible at a glance.
Output: Fixed crm-workspace-page.tsx and crm-pipeline-card.tsx.
</objective>

<execution_context>
@D:/Development/Fabersoft/hi-engenharia/code/.claude/get-shit-done/workflows/execute-plan.md
@D:/Development/Fabersoft/hi-engenharia/code/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

## Gap Context (from 04-UAT.md)

### Gap 1: Header scroll issue (major)
- Root cause: Header uses `sm:flex-row` with multiple items on right side without flex-wrap
- File: apps/web/components/platform/crm/crm-workspace-page.tsx, lines 86-119
- Issue: Right-side container has toggle buttons + "Nova oportunidade" button causing horizontal overflow on viewports 640px-900px

### Gap 2: Card missing responsible name (minor)
- Root cause: CrmPipelineCard renders only Avatar with initials, not the name text
- File: apps/web/components/platform/crm/crm-pipeline-card.tsx, lines 54-59
- Issue: owner.name exists in data but is never displayed
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix CRM header responsive layout</name>
  <files>apps/web/components/platform/crm/crm-workspace-page.tsx</files>
  <action>
    Fix the header overflow issue on medium viewports (640px-900px) by changing the breakpoint from `sm:` to `md:` for the horizontal flex layout.

    **Current code (line 86):**
    ```tsx
    <div className="flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between">
    ```

    **Change to:**
    ```tsx
    <div className="flex flex-col gap-4 md:flex-row md:items-center md:justify-between">
    ```

    Also update the nested flex container on line 90 to allow wrapping:
    ```tsx
    <div className="flex flex-wrap items-center gap-2">
    ```

    This ensures:
    1. Header stays vertical until md breakpoint (768px) instead of sm (640px)
    2. Toggle buttons + "Nova oportunidade" button wrap to new line if needed on narrow viewports
  </action>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <done>Header uses md:flex-row breakpoint and flex-wrap on right-side container, no horizontal scroll on viewports 640px-900px</done>
</task>

<task type="auto">
  <name>Task 2: Add responsible name to Kanban card</name>
  <files>apps/web/components/platform/crm/crm-pipeline-card.tsx</files>
  <action>
    Add the responsible person's name text after the Avatar in the card footer.

    **Current code (lines 54-59):**
    ```tsx
    <div className="flex items-center gap-1">
      <Avatar size="sm">
        <AvatarFallback>{owner?.initials ?? "??"}</AvatarFallback>
      </Avatar>
    </div>
    ```

    **Change to:**
    ```tsx
    <div className="flex items-center gap-1.5">
      <Avatar size="sm">
        <AvatarFallback>{owner?.initials ?? "??"}</AvatarFallback>
      </Avatar>
      <span className="truncate">{owner?.name ?? "Nao atribuido"}</span>
    </div>
    ```

    This ensures:
    1. Avatar displays initials (existing behavior)
    2. Owner name appears next to avatar in truncated text
    3. Falls back to "Nao atribuido" if no owner
  </action>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <done>Kanban card shows responsible person name as text after Avatar initials</done>
</task>

</tasks>

<verification>
1. Navigate to /crm on viewport 768px width
2. Verify header shows title, toggle buttons, and "Nova oportunidade" without horizontal scroll
3. Verify Kanban cards display owner name next to avatar initials
4. Run `pnpm typecheck` to confirm no type errors
</verification>

<success_criteria>
- [ ] Header no longer requires horizontal scroll on viewports 640px-900px
- [ ] Kanban cards show responsible person name alongside avatar
- [ ] No TypeScript errors
- [ ] UAT Test 6 (toggle views) and Test 8 (card display) would now pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-crm-e-pipeline-comercial/04-04-SUMMARY.md`
</output>
