---
phase: 01-shell-de-acesso-e-identidade
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - apps/web/app/(public)/login/actions.ts
  - apps/web/app/(public)/login/page.tsx
  - apps/web/components/auth/login-form.tsx
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
user_setup: []
must_haves:
  truths:
    - The login page accepts any email and password, stores the mock session, and sends the user to `/portal`.
    - The desktop login screen uses the split 50/50 layout while mobile falls back to a single-column centered form.
    - The login screen communicates that profile context is simulated and that profile switching happens inside the shell.
  artifacts:
    - apps/web/app/(public)/login/actions.ts
    - apps/web/app/(public)/login/page.tsx
    - apps/web/components/auth/login-form.tsx
  key_links:
    - `apps/web/app/(public)/login/actions.ts` writes the same `hi_portal_session` cookie contract defined in `apps/web/lib/mock-session.ts`.
    - `apps/web/app/(public)/login/page.tsx` composes `apps/web/components/auth/login-form.tsx` and the shared `BrandLogo` component.
---

<objective>
Construir a entrada simulada da plataforma com identidade visual da Hi Engenharia e redirecionamento para o portal.

Purpose: Satisfazer o acesso inicial da fase sem backend real, mantendo o fluxo alinhado ao shell que sera construido em paralelo.
Output: Rota `/login`, formulario reutilizavel e server action que grava a sessao mockada.
</objective>

<execution_context>
@D:/Development/Fabersoft/hi-engenharia/code/.codex/get-shit-done/workflows/execute-plan.md
@D:/Development/Fabersoft/hi-engenharia/code/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-shell-de-acesso-e-identidade/01-CONTEXT.md
@.planning/phases/01-shell-de-acesso-e-identidade/01-RESEARCH.md
@.planning/phases/01-shell-de-acesso-e-identidade/01-VALIDATION.md
@apps/web/lib/mock-session.ts
@apps/web/components/brand-logo.tsx
@packages/ui/src/components/button.tsx
@packages/ui/src/components/input.tsx
@packages/ui/src/components/label.tsx
@packages/ui/src/components/card.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the server action that creates the mock login session</name>
  <files>apps/web/app/(public)/login/actions.ts</files>
  <read_first>apps/web/lib/mock-session.ts, .planning/phases/01-shell-de-acesso-e-identidade/01-RESEARCH.md</read_first>
  <action>Create `apps/web/app/(public)/login/actions.ts` with a server action named `enterPortal`. It must read `email` and `password` from `FormData`, accept any non-empty values, serialize the mock session with `authenticated: true`, `email`, and `activeProfile: DEFAULT_PROFILE`, set the `hi_portal_session` cookie, and call `redirect("/portal")`. Do not add credential validation, API calls, or error handling for invalid credentials. The only validation should be required form fields in the UI.</action>
  <verify>rg -n "enterPortal" "apps/web/app/(public)/login/actions.ts" && rg -n "redirect\\(\"/portal\"\\)|redirect\\('/portal'\\)" "apps/web/app/(public)/login/actions.ts"</verify>
  <acceptance_criteria>
    - `apps/web/app/(public)/login/actions.ts` contains `enterPortal`
    - `apps/web/app/(public)/login/actions.ts` imports the serializer/helper from `apps/web/lib/mock-session.ts`
    - `apps/web/app/(public)/login/actions.ts` sets `activeProfile: DEFAULT_PROFILE`
  </acceptance_criteria>
  <done>The login route has a server action that creates the agreed mock session and redirects into the shell.</done>
</task>

<task type="auto">
  <name>Task 2: Build the reusable login form component with pending feedback</name>
  <files>apps/web/components/auth/login-form.tsx</files>
  <read_first>packages/ui/src/components/button.tsx, packages/ui/src/components/input.tsx, packages/ui/src/components/label.tsx, packages/ui/src/components/card.tsx, apps/web/app/(public)/login/actions.ts</read_first>
  <action>Create `apps/web/components/auth/login-form.tsx` as a client component that renders a card-based form with `Label`, `Input`, and shared `Button`. The fields must be `email` and `password`; both marked `required`. Use the `enterPortal` server action as the form `action`, add a short pending state via `useFormStatus`, and render the exact Portuguese helper copy `Qualquer e-mail e senha funcionam nesta fase.` plus a profile-context line that tells the user the initial simulated profile is `Administrador` and that profile switching happens after entering the portal.</action>
  <verify>rg -n "Qualquer e-mail e senha funcionam nesta fase\\." apps/web/components/auth/login-form.tsx && rg -n "Administrador" apps/web/components/auth/login-form.tsx</verify>
  <acceptance_criteria>
    - `apps/web/components/auth/login-form.tsx` contains fields named `email` and `password`
    - `apps/web/components/auth/login-form.tsx` contains the exact helper text `Qualquer e-mail e senha funcionam nesta fase.`
    - `apps/web/components/auth/login-form.tsx` contains a pending-state branch tied to `useFormStatus`
  </acceptance_criteria>
  <done>The login form is reusable, uses shared UI primitives, and exposes a visual submit state without real authentication logic.</done>
</task>

<task type="auto">
  <name>Task 3: Compose the responsive login page route</name>
  <files>apps/web/app/(public)/login/page.tsx</files>
  <read_first>apps/web/app/layout.tsx, apps/web/components/brand-logo.tsx, apps/web/components/auth/login-form.tsx, .planning/phases/01-shell-de-acesso-e-identidade/01-CONTEXT.md</read_first>
  <action>Create `apps/web/app/(public)/login/page.tsx` as the public route for `/login`. On desktop (`lg` and above), render a two-column layout where the left half centers the `BrandLogo` and `LoginForm`, and the right half uses the primary brand tone with white text for the Hi Engenharia statement. On mobile, hide the right panel entirely and keep the form vertically centered on a neutral background. Include only Portuguese UI copy. Do not add a profile selector on this page.</action>
  <verify>rg -n "login-form|LoginForm" "apps/web/app/(public)/login/page.tsx" && rg -n "BrandLogo" "apps/web/app/(public)/login/page.tsx"</verify>
  <acceptance_criteria>
    - `apps/web/app/(public)/login/page.tsx` contains `LoginForm`
    - `apps/web/app/(public)/login/page.tsx` contains `BrandLogo`
    - `apps/web/app/(public)/login/page.tsx` contains the exact responsive class string `hidden lg:flex`
    - `apps/web/app/(public)/login/page.tsx` does not contain the string `Escolha seu perfil`
  </acceptance_criteria>
  <done>The public login route matches the approved desktop/mobile layout and keeps profile switching inside the shell.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm --filter web lint`
- [ ] `pnpm --filter web typecheck`
- [ ] `rg -n "Qualquer e-mail e senha funcionam nesta fase\\." apps/web/components/auth/login-form.tsx`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The user can submit `/login` with any credentials and be redirected toward `/portal`
  </success_criteria>

<output>
After completion, create `.planning/phases/01-shell-de-acesso-e-identidade/01-02-SUMMARY.md`
</output>
