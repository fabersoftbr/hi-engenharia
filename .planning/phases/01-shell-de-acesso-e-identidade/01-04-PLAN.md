---
phase: 01-shell-de-acesso-e-identidade
plan: 04
type: execute
wave: 3
depends_on:
  - "02"
  - "03"
files_modified:
  - apps/web/app/(platform)/layout.tsx
  - apps/web/components/platform/app-header.tsx
  - apps/web/components/platform/app-sidebar.tsx
  - apps/web/components/platform/module-placeholder-page.tsx
  - apps/web/components/platform/platform-shell-provider.tsx
  - apps/web/components/platform/profile-switcher.tsx
  - apps/web/components/platform/restricted-module-state.tsx
autonomous: true
requirements:
  - ROLE-01
  - ROLE-02
  - AUTH-02
user_setup: []
must_haves:
  truths:
    - The active profile can be changed from the shell header and the sidebar updates immediately to reflect that profile's access map.
    - Unauthorized modules stay hidden in the sidebar but direct navigation to a restricted route shows an explicit blocked state inside the shell.
    - The shell persists the active simulated profile across navigation and reloads.
  artifacts:
    - apps/web/app/(platform)/layout.tsx
    - apps/web/components/platform/platform-shell-provider.tsx
    - apps/web/components/platform/profile-switcher.tsx
    - apps/web/components/platform/restricted-module-state.tsx
    - apps/web/components/platform/app-sidebar.tsx
    - apps/web/components/platform/app-header.tsx
  key_links:
    - `apps/web/components/platform/platform-shell-provider.tsx` reads the same profile keys exported by `apps/web/lib/platform-config.ts`.
    - `apps/web/components/platform/profile-switcher.tsx`, `apps/web/components/platform/app-sidebar.tsx`, and `apps/web/components/platform/module-placeholder-page.tsx` all consume the same active profile state.
---

<objective>
Aplicar a diferenciacao visual por perfil e os estados de acesso restrito sem criar permissao real.

Purpose: Fechar a fase com comportamento coerente de perfil para que o shell pareca um produto operacional desde a primeira entrega.
Output: Provider de shell, seletor de perfil, sidebar filtrada por perfil e estado bloqueado para navegacao direta em rotas restritas.
</objective>

<execution_context>
@D:/Development/Fabersoft/hi-engenharia/code/.codex/get-shit-done/workflows/execute-plan.md
@D:/Development/Fabersoft/hi-engenharia/code/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-shell-de-acesso-e-identidade/01-CONTEXT.md
@.planning/phases/01-shell-de-acesso-e-identidade/01-RESEARCH.md
@.planning/phases/01-shell-de-acesso-e-identidade/01-VALIDATION.md
@apps/web/lib/platform-config.ts
@apps/web/lib/mock-session.ts
@apps/web/components/platform/app-sidebar.tsx
@apps/web/components/platform/app-header.tsx
@apps/web/components/platform/module-placeholder-page.tsx
@packages/ui/src/components/dropdown-menu.tsx
@packages/ui/src/components/avatar.tsx
@packages/ui/src/components/badge.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Introduce the shared shell-state provider for active profile and sidebar state</name>
  <files>apps/web/app/(platform)/layout.tsx, apps/web/components/platform/platform-shell-provider.tsx</files>
  <read_first>apps/web/lib/platform-config.ts, apps/web/lib/mock-session.ts, apps/web/app/(platform)/layout.tsx, .planning/phases/01-shell-de-acesso-e-identidade/01-RESEARCH.md</read_first>
  <action>Create `apps/web/components/platform/platform-shell-provider.tsx` as the client-side state container for the shell. It must manage `activeProfile`, `sidebarCollapsed`, and `mobileNavOpen`. Initialize `activeProfile` from the mock session cookie default, persist updates under the local key `hi-active-profile`, and expose helpers such as `setActiveProfile`, `toggleSidebar`, and `setMobileNavOpen`. The provider must only use the four profile keys already defined in `apps/web/lib/platform-config.ts`: `admin`, `commercial`, `partner`, and `operations`. Update `apps/web/app/(platform)/layout.tsx` so the existing shell chrome is wrapped in `PlatformShellProvider`, making the active profile available to the header, sidebar, and module placeholder renderer on every internal route.</action>
  <verify>rg -n "hi-active-profile" apps/web/components/platform/platform-shell-provider.tsx && rg -n "admin|commercial|partner|operations" apps/web/components/platform/platform-shell-provider.tsx</verify>
  <acceptance_criteria>
    - `apps/web/components/platform/platform-shell-provider.tsx` contains the storage key `hi-active-profile`
    - `apps/web/components/platform/platform-shell-provider.tsx` exports `setActiveProfile`
    - `apps/web/components/platform/platform-shell-provider.tsx` exports `toggleSidebar`
    - `apps/web/app/(platform)/layout.tsx` contains `PlatformShellProvider`
  </acceptance_criteria>
  <done>The shell has one client-side provider that controls profile and sidebar state consistently across the header and navigation.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the static header account area with a real profile switcher</name>
  <files>apps/web/components/platform/profile-switcher.tsx, apps/web/components/platform/app-header.tsx, apps/web/components/platform/app-sidebar.tsx</files>
  <read_first>apps/web/components/platform/platform-shell-provider.tsx, apps/web/lib/platform-config.ts, apps/web/components/platform/app-header.tsx, apps/web/components/platform/app-sidebar.tsx, packages/ui/src/components/dropdown-menu.tsx</read_first>
  <action>Create `apps/web/components/platform/profile-switcher.tsx` as the avatar-dropdown control that lists the four Portuguese profile labels: `Administrador`, `Comercial interno`, `Afiliado/Parceiro externo`, and `Engenharia/Operacao`. Update `apps/web/components/platform/app-header.tsx` so the right side shows the active profile in a `Badge` and uses `ProfileSwitcher` instead of the static dropdown scaffold. Update `apps/web/components/platform/app-sidebar.tsx` to filter visible modules from `platform-config.ts` according to the active profile supplied by the provider. The exact visibility map must match the phase context: `admin` sees all modules; `commercial` sees `portal`, `orcamentos`, `crm`, `propostas`, and `tabela-de-precos`; `partner` sees `portal` and `orcamentos`; `operations` sees `portal`, `anteprojetos`, `projetos`, `obras`, and `drive`.</action>
  <verify>rg -n "Administrador|Comercial interno|Afiliado/Parceiro externo|Engenharia/Operacao" apps/web/components/platform/profile-switcher.tsx && rg -n "activeProfile|filter\\(" apps/web/components/platform/app-sidebar.tsx</verify>
  <acceptance_criteria>
    - `apps/web/components/platform/profile-switcher.tsx` contains the four exact profile labels listed in the action text
    - `apps/web/components/platform/app-header.tsx` imports and renders `ProfileSwitcher`
    - `apps/web/components/platform/app-sidebar.tsx` contains `activeProfile`
    - `apps/web/components/platform/app-sidebar.tsx` contains `filter(`
    - `apps/web/components/platform/app-header.tsx` renders a `Badge` for the active profile
  </acceptance_criteria>
  <done>Profile switching is visible in the shell UI and the sidebar responds immediately to the active role.</done>
</task>

<task type="auto">
  <name>Task 3: Add the restricted-route state without reintroducing hidden menu items</name>
  <files>apps/web/components/platform/restricted-module-state.tsx, apps/web/components/platform/module-placeholder-page.tsx</files>
  <read_first>apps/web/components/platform/platform-shell-provider.tsx, apps/web/lib/platform-config.ts, apps/web/components/platform/module-placeholder-page.tsx, .planning/phases/01-shell-de-acesso-e-identidade/01-CONTEXT.md</read_first>
  <action>Create `apps/web/components/platform/restricted-module-state.tsx` as the blocked-state body for unauthorized module routes. Update `apps/web/components/platform/module-placeholder-page.tsx` so it reads the active profile from the provider, checks access with the exact visibility map from `platform-config.ts`, and either renders the normal placeholder or the restricted state. The restricted state must keep the shell chrome visible, render Portuguese copy that tells the user the current profile does not have access to that area, and include a clear action back to `/portal` with button text `Voltar ao Portal`. Keep unauthorized modules hidden in the sidebar; do not add disabled sidebar entries.</action>
  <verify>rg -n "Voltar ao Portal" apps/web/components/platform/restricted-module-state.tsx && rg -n "nao possui acesso|sem acesso" apps/web/components/platform/restricted-module-state.tsx</verify>
  <acceptance_criteria>
    - `apps/web/components/platform/restricted-module-state.tsx` contains the exact CTA text `Voltar ao Portal`
    - `apps/web/components/platform/module-placeholder-page.tsx` conditionally renders `RestrictedModuleState`
    - `apps/web/components/platform/app-sidebar.tsx` does not contain the string `aria-disabled`
  </acceptance_criteria>
  <done>Restricted access is represented through route-level blocked states while the sidebar remains clean and role-specific.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm lint && pnpm typecheck && pnpm build`
- [ ] switch profile in the shell and confirm sidebar updates
- [ ] type a restricted module URL directly and confirm the blocked state appears with `Voltar ao Portal`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The shell now communicates role-specific access through both filtered navigation and explicit restricted-route states
  </success_criteria>

<output>
After completion, create `.planning/phases/01-shell-de-acesso-e-identidade/01-04-SUMMARY.md`
</output>
