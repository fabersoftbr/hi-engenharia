---
phase: 01-shell-de-acesso-e-identidade
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/app/page.tsx
  - apps/web/components/brand-logo.tsx
  - apps/web/lib/mock-session.ts
  - apps/web/lib/platform-config.ts
  - packages/ui/src/components/avatar.tsx
  - packages/ui/src/components/badge.tsx
  - packages/ui/src/components/breadcrumb.tsx
  - packages/ui/src/components/card.tsx
  - packages/ui/src/components/dropdown-menu.tsx
  - packages/ui/src/components/input.tsx
  - packages/ui/src/components/label.tsx
  - packages/ui/src/components/separator.tsx
  - packages/ui/src/components/sheet.tsx
  - packages/ui/src/components/sidebar.tsx
  - packages/ui/src/components/tooltip.tsx
autonomous: true
requirements:
  - AUTH-01
  - PORT-02
  - PORT-04
  - ROLE-01
user_setup:
  - Provide the official Hi Engenharia logo files at `apps/web/public/brand/hi-logo-full.svg` and `apps/web/public/brand/hi-logo-mark.svg` when available. Until then, keep the text-only fallback in `BrandLogo`.
must_haves:
  truths:
    - The app has a single source of truth for module routes, group labels, breadcrumb labels, and profile visibility.
    - Root navigation redirects to `/login` or `/portal` based on the mock session cookie instead of showing the starter placeholder.
    - Shared shadcn navigation and form primitives are available from `@workspace/ui/components/*` for the login and shell plans.
  artifacts:
    - apps/web/lib/platform-config.ts
    - apps/web/lib/mock-session.ts
    - apps/web/components/brand-logo.tsx
    - apps/web/app/page.tsx
    - packages/ui/src/components/sidebar.tsx
  key_links:
    - `apps/web/app/page.tsx` imports the mock session helper from `apps/web/lib/mock-session.ts`.
    - `apps/web/lib/platform-config.ts` exports the exact module/group/profile data consumed later by the login, shell, and access-control plans.
---

<objective>
Estabelecer a fundacao tecnica e visual da Fase 1 antes da implementacao de login e shell.

Purpose: Garantir que todas as rotas, perfis, componentes shadcn e helpers de sessao mockada estejam definidos uma vez e reutilizados pelas fases seguintes.
Output: Primitivas shadcn compartilhadas em `@workspace/ui`, config central de navegacao/perfis, helper de sessao mockada, componente de marca e redirect do root route.
</objective>

<execution_context>
@D:/Development/Fabersoft/hi-engenharia/code/.codex/get-shit-done/workflows/execute-plan.md
@D:/Development/Fabersoft/hi-engenharia/code/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-shell-de-acesso-e-identidade/01-CONTEXT.md
@.planning/phases/01-shell-de-acesso-e-identidade/01-RESEARCH.md
@.planning/phases/01-shell-de-acesso-e-identidade/01-VALIDATION.md
@apps/web/components.json
@packages/ui/components.json
@packages/ui/src/styles/globals.css
@packages/ui/src/components/button.tsx
@apps/web/app/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the shared shadcn primitives required by the shell phase</name>
  <files>packages/ui/src/components/sidebar.tsx, packages/ui/src/components/breadcrumb.tsx, packages/ui/src/components/dropdown-menu.tsx, packages/ui/src/components/avatar.tsx, packages/ui/src/components/badge.tsx, packages/ui/src/components/separator.tsx, packages/ui/src/components/tooltip.tsx, packages/ui/src/components/sheet.tsx, packages/ui/src/components/input.tsx, packages/ui/src/components/label.tsx, packages/ui/src/components/card.tsx</files>
  <read_first>apps/web/components.json, packages/ui/components.json, packages/ui/src/styles/globals.css, packages/ui/src/components/button.tsx</read_first>
  <action>Run `pnpm dlx shadcn@latest add sidebar breadcrumb dropdown-menu avatar badge separator tooltip sheet input label card -c apps/web` from the repo root so the generated components respect the `apps/web/components.json` aliases that point `ui` to `@workspace/ui/components`. After generation, read every added file under `packages/ui/src/components/` and fix any hardcoded `@/components/ui/...` or `@/lib/utils` imports so they point to `@workspace/ui/components/...` and `@workspace/ui/lib/utils`. Keep the generated components on semantic tokens from `packages/ui/src/styles/globals.css`; do not add raw color classes or custom layout wrappers that bypass the shadcn primitives.</action>
  <verify>rg -n "export .*Sidebar|function Sidebar" packages/ui/src/components/sidebar.tsx && rg -n "Breadcrumb" packages/ui/src/components/breadcrumb.tsx && rg -n "DropdownMenu" packages/ui/src/components/dropdown-menu.tsx</verify>
  <acceptance_criteria>
    - `packages/ui/src/components/sidebar.tsx` exists and contains `SidebarProvider`
    - `packages/ui/src/components/breadcrumb.tsx` exists and contains `BreadcrumbPage`
    - `packages/ui/src/components/input.tsx` exists and imports `cn` from `@workspace/ui/lib/utils`
    - no added file in `packages/ui/src/components/` contains `@/components/ui/`
  </acceptance_criteria>
  <done>The shared shadcn primitives required by the login and shell plans exist in `packages/ui/src/components/` and use workspace-safe imports.</done>
</task>

<task type="auto">
  <name>Task 2: Create the shared route, profile, and mock-session source of truth</name>
  <files>apps/web/lib/platform-config.ts, apps/web/lib/mock-session.ts, apps/web/components/brand-logo.tsx</files>
  <read_first>.planning/phases/01-shell-de-acesso-e-identidade/01-CONTEXT.md, .planning/REQUIREMENTS.md, apps/web/tsconfig.json, packages/ui/src/styles/globals.css</read_first>
  <action>Create `apps/web/lib/platform-config.ts` with typed exports for the exact Phase 1 modules: `portal`, `orcamentos`, `crm`, `anteprojetos`, `projetos`, `obras`, `drive`, `comunicacao`, `propostas`, and `tabela-de-precos`. Include each route path, group label, breadcrumb title, and the exact profile visibility map from the phase context: `admin`, `commercial`, `partner`, and `operations`. Create `apps/web/lib/mock-session.ts` with `SESSION_COOKIE_NAME = "hi_portal_session"`, a `MockProfile` union for those four profile keys, `DEFAULT_PROFILE = "admin"`, and helpers to parse/serialize `{ authenticated: true, email: string, activeProfile: MockProfile }`. Create `apps/web/components/brand-logo.tsx` as an app-only component that reserves `/brand/hi-logo-full.svg` and `/brand/hi-logo-mark.svg`, renders those assets when the files exist, and otherwise renders the text fallback `HI Engenharia` without inventing a fake graphic.</action>
  <verify>rg -n "SESSION_COOKIE_NAME = \"hi_portal_session\"" apps/web/lib/mock-session.ts && rg -n "\"tabela-de-precos\"" apps/web/lib/platform-config.ts && rg -n "HI Engenharia" apps/web/components/brand-logo.tsx</verify>
  <acceptance_criteria>
    - `apps/web/lib/mock-session.ts` contains `DEFAULT_PROFILE = "admin"`
    - `apps/web/lib/platform-config.ts` contains `portal`, `orcamentos`, `crm`, `anteprojetos`, `projetos`, `obras`, `drive`, `comunicacao`, `propostas`, and `tabela-de-precos`
    - `apps/web/lib/platform-config.ts` contains profile keys `admin`, `commercial`, `partner`, and `operations`
    - `apps/web/components/brand-logo.tsx` references `/brand/hi-logo-full.svg` and `/brand/hi-logo-mark.svg`
  </acceptance_criteria>
  <done>The phase has one authoritative config for modules, profile access, breadcrumbs, and mock-session behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Replace the starter root page with a redirect entrypoint</name>
  <files>apps/web/app/page.tsx</files>
  <read_first>apps/web/app/page.tsx, apps/web/lib/mock-session.ts, .planning/phases/01-shell-de-acesso-e-identidade/01-RESEARCH.md</read_first>
  <action>Overwrite `apps/web/app/page.tsx` so it becomes a server component that reads the `hi_portal_session` cookie through the helper in `apps/web/lib/mock-session.ts` and immediately calls `redirect("/portal")` when a valid mock session exists, or `redirect("/login")` when it does not. Remove the starter `Project ready!` markup entirely so Phase 1 never exposes the scaffold page again.</action>
  <verify>rg -n "redirect\\(\"/portal\"\\)|redirect\\('/portal'\\)" apps/web/app/page.tsx && rg -n "redirect\\(\"/login\"\\)|redirect\\('/login'\\)" apps/web/app/page.tsx</verify>
  <acceptance_criteria>
    - `apps/web/app/page.tsx` imports `redirect` from `next/navigation`
    - `apps/web/app/page.tsx` imports the session helper from `apps/web/lib/mock-session.ts`
    - `apps/web/app/page.tsx` does not contain the string `Project ready!`
  </acceptance_criteria>
  <done>The root route is now a Phase 1 entrypoint that forwards users into the login or portal flow.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm --filter web lint && pnpm --filter @workspace/ui lint`
- [ ] `pnpm --filter web typecheck && pnpm --filter @workspace/ui typecheck`
- [ ] `rg -n "Project ready!" apps/web/app/page.tsx` returns no matches
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Shared shell primitives, route config, and mock session helpers exist before the login and shell plans start
  </success_criteria>

<output>
After completion, create `.planning/phases/01-shell-de-acesso-e-identidade/01-01-SUMMARY.md`
</output>
