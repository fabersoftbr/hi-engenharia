---
phase: 08
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/web/package.json
  - apps/web/vitest.config.ts
  - apps/web/test/setup.ts
  - apps/web/components/platform/states/__tests__/empty-states.test.tsx
  - apps/web/components/platform/states/__tests__/loading-and-feedback.test.tsx
  - apps/web/components/platform/responsive/__tests__/responsive-layouts.test.tsx
autonomous: true
requirements:
  - STAT-01
  - STAT-02
  - STAT-03
  - RESP-01
must_haves:
  truths:
    - "Vitest roda em jsdom dentro de apps/web com aliases @/ e @workspace/ui resolvidos pelo vitest.config.ts"
    - "apps/web/test/setup.ts registra jest-dom e polyfills para matchMedia antes dos testes responsivos"
    - "empty-states.test.tsx cobre EmptyState padrao, acao opcional e copy de vazio/sem resultados"
    - "loading-and-feedback.test.tsx cobre useSimulatedLoading e os helpers showSuccessToast, showInfoToast, showErrorToast, showUndoToast e showPdfReadyToast"
    - "responsive-layouts.test.tsx valida breakpoint md (768px), uso de Sheet/Tooltip e o contrato de tabelas responsivas"
  artifacts:
    - path: apps/web/vitest.config.ts
      provides: "Vitest config for jsdom, aliases, React transform, and shared setup"
    - path: apps/web/test/setup.ts
      provides: "jest-dom bootstrap and matchMedia polyfill required by next-themes and useIsMobile"
    - path: apps/web/components/platform/states/__tests__/empty-states.test.tsx
      provides: "Automated assertions for empty and no-results states"
    - path: apps/web/components/platform/states/__tests__/loading-and-feedback.test.tsx
      provides: "Automated assertions for loading delays and toast helper behavior"
    - path: apps/web/components/platform/responsive/__tests__/responsive-layouts.test.tsx
      provides: "Automated assertions for responsive table and overlay contracts"
  key_links:
    - "apps/web/vitest.config.ts points setupFiles to apps/web/test/setup.ts"
    - "apps/web/test/setup.ts must prepare window.matchMedia before responsive-layouts.test.tsx runs"
    - "loading-and-feedback.test.tsx imports helpers from @/lib/toast-helpers and @/lib/use-simulated-loading"
    - "responsive-layouts.test.tsx is the contract that later plans 03, 04, and 05 must keep green"
---

<objective>
Register the Wave 0 validation baseline for Phase 8 and keep the test harness executable inside apps/web.

Purpose: Give execute-phase a fast feedback loop before the visual-state and responsive plans land.
Output: Working Vitest config plus three targeted specs that protect STAT-01, STAT-02, STAT-03, and RESP-01 primitives.
</objective>

<execution_context>
@D:/Development/Fabersoft/hi-engenharia/code/.claude/get-shit-done/workflows/execute-plan.md
@D:/Development/Fabersoft/hi-engenharia/code/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-estados-responsividade-e-jornada-completa/08-CONTEXT.md
@.planning/phases/08-estados-responsividade-e-jornada-completa/08-RESEARCH.md
@.planning/phases/08-estados-responsividade-e-jornada-completa/08-VALIDATION.md
@.claude/skills/shadcn/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure the apps/web Vitest harness for UI-state and responsive tests</name>
  <files>
    - apps/web/package.json
    - apps/web/vitest.config.ts
    - apps/web/test/setup.ts
  </files>
  <read_first>
    - AGENTS.md
    - apps/web/package.json
    - apps/web/tsconfig.json
    - .planning/phases/08-estados-responsividade-e-jornada-completa/08-VALIDATION.md
  </read_first>
  <action>
    Ensure `apps/web/package.json` includes the test-time dependencies already referenced by the phase research: `vitest`, `jsdom`, `@testing-library/react`, `@testing-library/jest-dom`, and `@vitejs/plugin-react`.

    Create or update `apps/web/vitest.config.ts` with these exact behaviors:
    - use `defineConfig`
    - register `react()` from `@vitejs/plugin-react`
    - set `test.environment` to `"jsdom"`
    - set `test.setupFiles` to `["./test/setup.ts"]`
    - resolve `@/` to `apps/web` and `@workspace/ui` to the local workspace package
    - keep tests running from the `apps/web` boundary only

    Create or update `apps/web/test/setup.ts` so it:
    - imports `@testing-library/jest-dom/vitest`
    - polyfills `window.matchMedia`
    - exposes `ResizeObserver` or a no-op shim if the responsive tests need it
    - keeps all setup logic in English comments only
  </action>
  <verify>
    <automated>cd D:/Development/Fabersoft/hi-engenharia/code/apps/web && rg -n "jsdom|setupFiles|@vitejs/plugin-react|@/|@workspace/ui" vitest.config.ts && rg -n "jest-dom|matchMedia|ResizeObserver" test/setup.ts && pnpm vitest run --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/vitest.config.ts` contains `environment: "jsdom"`
    - `apps/web/vitest.config.ts` contains `setupFiles`
    - `apps/web/vitest.config.ts` contains `@vitejs/plugin-react`
    - `apps/web/test/setup.ts` contains `@testing-library/jest-dom/vitest`
    - `apps/web/test/setup.ts` contains `matchMedia`
    - `apps/web/package.json` contains `vitest`
    - `apps/web/package.json` contains `jsdom`
    - `cd apps/web && pnpm vitest run --reporter=verbose` exits 0
  </acceptance_criteria>
  <done>The apps/web test harness can execute Phase 8 UI-state and responsive specs without external setup.</done>
</task>

<task type="auto">
  <name>Task 2: Add the targeted Phase 8 baseline specs for states, feedback, and responsiveness</name>
  <files>
    - apps/web/components/platform/states/__tests__/empty-states.test.tsx
    - apps/web/components/platform/states/__tests__/loading-and-feedback.test.tsx
    - apps/web/components/platform/responsive/__tests__/responsive-layouts.test.tsx
  </files>
  <read_first>
    - .planning/phases/08-estados-responsividade-e-jornada-completa/08-CONTEXT.md
    - .planning/phases/08-estados-responsividade-e-jornada-completa/08-RESEARCH.md
    - .planning/phases/08-estados-responsividade-e-jornada-completa/08-VALIDATION.md
    - apps/web/components/platform/states/empty-state.tsx
    - apps/web/lib/toast-helpers.ts
    - apps/web/lib/use-simulated-loading.ts
    - packages/ui/src/hooks/use-mobile.ts
  </read_first>
  <action>
    Write the three baseline specs with these exact assertions:

    1. `empty-states.test.tsx`
    - render `EmptyState` with title-only usage
    - render `EmptyState` with icon, description, and action button
    - assert the copy `Nenhum comunicado publicado`, `Nenhum resultado`, and the action label `Limpar filtros`

    2. `loading-and-feedback.test.tsx`
    - use fake timers to prove `useSimulatedLoading()` starts as `true` and flips after 800ms
    - mock `sonner` and assert:
      - `showSuccessToast("Operacao concluida")` uses duration `3000`
      - `showInfoToast("Informacao importante")` uses duration `4000`
      - `showErrorToast("Algo deu errado")` uses duration `5000`
      - `showUndoToast("Etapa alterada", onUndo)` renders the action label `Desfazer`
      - `showPdfReadyToast(onDownload)` renders the action label `Baixar`

    3. `responsive-layouts.test.tsx`
    - validate the `useIsMobile` breakpoint at 768px
    - assert the responsive table pattern keeps hidden classes such as `hidden md:table-cell`
    - assert the mobile-overlay contract uses `Sheet`, `SheetContent`, and a title node
    - assert the tooltip provider supports the 300ms delay contract

    Do not create the `/jornada` spec in this task. That file is intentionally deferred to plan `08-06`.
  </action>
  <verify>
    <automated>cd D:/Development/Fabersoft/hi-engenharia/code/apps/web && rg -n "Nenhum comunicado publicado|Nenhum resultado|Limpar filtros" components/platform/states/__tests__/empty-states.test.tsx && rg -n "showSuccessToast|showInfoToast|showErrorToast|showUndoToast|showPdfReadyToast|3000|4000|5000|Desfazer|Baixar|800" components/platform/states/__tests__/loading-and-feedback.test.tsx && rg -n "useIsMobile|768|hidden md:table-cell|Sheet|Tooltip" components/platform/responsive/__tests__/responsive-layouts.test.tsx && pnpm vitest run --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/components/platform/states/__tests__/empty-states.test.tsx` contains `Nenhum comunicado publicado`
    - `apps/web/components/platform/states/__tests__/empty-states.test.tsx` contains `Nenhum resultado`
    - `apps/web/components/platform/states/__tests__/loading-and-feedback.test.tsx` contains `showSuccessToast`
    - `apps/web/components/platform/states/__tests__/loading-and-feedback.test.tsx` contains `showPdfReadyToast`
    - `apps/web/components/platform/states/__tests__/loading-and-feedback.test.tsx` contains `3000` and `4000` and `5000`
    - `apps/web/components/platform/responsive/__tests__/responsive-layouts.test.tsx` contains `useIsMobile`
    - `apps/web/components/platform/responsive/__tests__/responsive-layouts.test.tsx` contains `hidden md:table-cell`
    - `apps/web/components/platform/responsive/__tests__/responsive-layouts.test.tsx` contains `Sheet`
    - `cd apps/web && pnpm vitest run --reporter=verbose` exits 0
  </acceptance_criteria>
  <done>The Phase 8 baseline specs protect the primitives that later plans must adopt without regressions.</done>
</task>

</tasks>

<verification>
After completing all tasks:
- `cd apps/web && pnpm vitest run --reporter=verbose`
- `cd apps/web && rg -n "jsdom|setupFiles" vitest.config.ts`
- `cd apps/web && rg -n "matchMedia" test/setup.ts`
- `cd apps/web && rg -n "showSuccessToast|showPdfReadyToast|useIsMobile" components/platform`
</verification>

<success_criteria>
- The apps/web Vitest harness runs locally without extra setup
- Empty-state, loading/toast, and responsive contract specs exist and pass
- Later plans can rely on the Wave 0 suite before shipping UI changes
- The journey-page test remains explicitly deferred to plan 08-06, matching 08-VALIDATION
</success_criteria>

<output>
After completion, create `.planning/phases/08-estados-responsividade-e-jornada-completa/08-00-SUMMARY.md`
</output>
