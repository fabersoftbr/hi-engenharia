---
phase: 08-estados-responsividade-e-jornada-completa
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/web/package.json
  - apps/web/vitest.config.ts
  - apps/web/test/setup.ts
  - apps/web/components/platform/states/__tests__/loading-and-feedback.test.tsx
  - apps/web/components/platform/states/__tests__/empty-states.test.tsx
  - apps/web/components/platform/responsive/__tests__/responsive-layouts.test.tsx
autonomous: true
requirements:
  - STAT-01
  - STAT-02
  - STAT-03
  - RESP-01
user_setup: []
must_haves:
  truths:
    - "Phase 8 execution gets a real Wave 0 harness instead of relying only on lint, typecheck, and manual UI review."
    - "The web app can run fast targeted specs for loading and feedback, empty and no-results states, and responsive layout contracts before the visual refactors start."
    - "Every later Phase 8 plan can point to concrete Vitest files required by `08-VALIDATION.md`."
  artifacts:
    - path: "apps/web/vitest.config.ts"
      provides: "Vitest config isolated to the web app"
      contains: "defineConfig"
    - path: "apps/web/test/setup.ts"
      provides: "Testing Library and DOM bootstrap"
      contains: "@testing-library/jest-dom"
    - path: "apps/web/components/platform/states/__tests__/loading-and-feedback.test.tsx"
      provides: "STAT-01 and STAT-03 spec target"
      contains: "describe(\"loading-and-feedback\""
    - path: "apps/web/components/platform/responsive/__tests__/responsive-layouts.test.tsx"
      provides: "RESP-01 spec target"
      contains: "describe(\"responsive-layouts\""
  key_links:
    - from: "apps/web/package.json"
      to: "apps/web/vitest.config.ts"
      via: "test script and devDependencies"
      pattern: "\"test\": \"vitest run\""
    - from: "apps/web/components/platform/states/__tests__/empty-states.test.tsx"
      to: "packages/ui/src/components/empty-state.tsx"
      via: "empty and no-results assertions"
      pattern: "EmptyState"
    - from: "apps/web/components/platform/responsive/__tests__/responsive-layouts.test.tsx"
      to: "packages/ui/src/components/data-table.tsx"
      via: "responsive contract assertions"
      pattern: "DataTable"
---

<objective>
Create the mandatory Wave 0 validation harness before any Phase 8 UI polish work starts.

Purpose: Close the checker blocker from `08-VALIDATION.md` by putting fast, targeted automation in place for loading, empty/no-results, feedback, and responsive layout behavior.
Output: `vitest` configured for `apps/web`, a shared test setup file, and the three required targeted spec files.
</objective>

<execution_context>
@D:/Development/Fabersoft/hi-engenharia/code/.codex/get-shit-done/workflows/execute-plan.md
@D:/Development/Fabersoft/hi-engenharia/code/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-estados-responsividade-e-jornada-completa/08-CONTEXT.md
@.planning/phases/08-estados-responsividade-e-jornada-completa/08-RESEARCH.md
@.planning/phases/08-estados-responsividade-e-jornada-completa/08-VALIDATION.md
@apps/web/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure the web-app Vitest runner and shared DOM test bootstrap</name>
  <files>apps/web/package.json, apps/web/vitest.config.ts, apps/web/test/setup.ts</files>
  <read_first>.planning/phases/08-estados-responsividade-e-jornada-completa/08-VALIDATION.md, apps/web/package.json, packages/ui/package.json</read_first>
  <acceptance_criteria>
- `rg -n "\"test\": \"vitest run\"" apps/web/package.json` returns a match.
- `rg -n "\"vitest\"|\"jsdom\"|\"@testing-library/react\"|\"@testing-library/jest-dom\"|\"@testing-library/user-event\"" apps/web/package.json` returns matches.
- `rg -n "defineConfig|environment: \"jsdom\"|setupFiles" apps/web/vitest.config.ts` returns matches.
- `rg -n "@testing-library/jest-dom" apps/web/test/setup.ts` returns a match.
  </acceptance_criteria>
  <action>Set up the Wave 0 harness exactly as required by `08-VALIDATION.md`. Add the dev dependencies `vitest`, `jsdom`, `@testing-library/react`, `@testing-library/jest-dom`, and `@testing-library/user-event` to `apps/web/package.json`, plus the exact script `"test": "vitest run"`. Create `apps/web/vitest.config.ts` with `defineConfig` from `vitest/config`, `test.environment = "jsdom"`, and `setupFiles = ["./test/setup.ts"]`. Create `apps/web/test/setup.ts` to import `@testing-library/jest-dom` and only the small DOM polyfills needed by the existing Next.js and shadcn components. Do not introduce watch-mode defaults, root-level test config, or cross-workspace test scripts in this plan.</action>
  <verify>
    <automated>pnpm --filter web lint && pnpm --filter web typecheck</automated>
  </verify>
  <done>The `web` app can execute component tests locally through `pnpm --filter web test`, and the harness is isolated to `apps/web`.</done>
</task>

<task type="auto">
  <name>Task 2: Create the targeted loading, feedback, empty, and no-results specs from the validation contract</name>
  <files>apps/web/components/platform/states/__tests__/loading-and-feedback.test.tsx, apps/web/components/platform/states/__tests__/empty-states.test.tsx</files>
  <read_first>.planning/phases/08-estados-responsividade-e-jornada-completa/08-VALIDATION.md, .planning/phases/08-estados-responsividade-e-jornada-completa/08-CONTEXT.md, apps/web/components/platform/portal-dashboard.tsx, apps/web/components/platform/budget-requests/budget-requests-list-page.tsx</read_first>
  <acceptance_criteria>
- `rg -n "describe\\(\"loading-and-feedback\"|describe\\(\"empty-states\"" apps/web/components/platform/states/__tests__/loading-and-feedback.test.tsx apps/web/components/platform/states/__tests__/empty-states.test.tsx` returns matches.
- `rg -n "TableSkeleton|CardGridSkeleton|PipelineSkeleton|DetailSkeleton|showSuccessToast|showInfoToast|showErrorToast|showUndoToast|showPdfReadyToast|useSimulatedLoading" apps/web/components/platform/states/__tests__/loading-and-feedback.test.tsx` returns matches.
- `rg -n "EmptyState|Nenhum resultado|Nenhum comunicado publicado|Fazer upload" apps/web/components/platform/states/__tests__/empty-states.test.tsx` returns matches.
  </acceptance_criteria>
  <action>Create the two state specs exactly named by `08-VALIDATION.md`. `loading-and-feedback.test.tsx` must cover the four shared skeletons, the development-only simulated loading helper contract, and the exported toast helpers `showSuccessToast`, `showInfoToast`, `showErrorToast`, `showUndoToast`, and `showPdfReadyToast`. `empty-states.test.tsx` must cover the shared `EmptyState`, one no-results branch, and one hide-empty-section rule so later screen adoption work cannot regress the locked copy and action patterns. Keep the specs fast, component-level, and free of route or E2E dependencies.</action>
  <verify>
    <automated>pnpm --dir apps/web vitest run components/platform/states/__tests__/loading-and-feedback.test.tsx components/platform/states/__tests__/empty-states.test.tsx</automated>
  </verify>
  <done>The phase now has stable spec targets for shared loading, feedback, empty, and no-results behavior before the UI refactors begin.</done>
</task>

<task type="auto">
  <name>Task 3: Add the responsive layout spec that guards shell, table, detail, and pipeline behavior</name>
  <files>apps/web/components/platform/responsive/__tests__/responsive-layouts.test.tsx</files>
  <read_first>.planning/phases/08-estados-responsividade-e-jornada-completa/08-VALIDATION.md, .planning/phases/08-estados-responsividade-e-jornada-completa/08-CONTEXT.md, packages/ui/src/components/data-table.tsx, packages/ui/src/hooks/use-mobile.ts</read_first>
  <acceptance_criteria>
- `rg -n "describe\\(\"responsive-layouts\"" apps/web/components/platform/responsive/__tests__/responsive-layouts.test.tsx` returns a match.
- `rg -n "useIsMobile|DataTable|h-16|Tabs|Sheet|Theme claro|Theme escuro|Seguir sistema" apps/web/components/platform/responsive/__tests__/responsive-layouts.test.tsx` returns matches.
  </acceptance_criteria>
  <action>Create `responsive-layouts.test.tsx` as the shared RESP-01 target for the rest of Phase 8. Cover the locked `md` breakpoint contract, the `h-16` header requirement, responsive table visibility behavior, the mobile pipeline tabs branch, and the theme-switcher options `Theme claro`, `Theme escuro`, and `Seguir sistema`. Keep the assertions narrow and deterministic so later plans can refactor shared layout code without relying on slow browser resize automation.</action>
  <verify>
    <automated>pnpm --dir apps/web vitest run components/platform/responsive/__tests__/responsive-layouts.test.tsx</automated>
  </verify>
  <done>Phase 8 now has one fast responsive-layout spec that later shell, table, detail, and pipeline plans can keep green.</done>
</task>

</tasks>

<verification>
Run `pnpm --filter web lint && pnpm --filter web typecheck && pnpm --dir apps/web vitest run components/platform/states/__tests__/loading-and-feedback.test.tsx components/platform/states/__tests__/empty-states.test.tsx components/platform/responsive/__tests__/responsive-layouts.test.tsx`.
</verification>

<success_criteria>
Wave 0 exists on disk, the targeted spec files required by `08-VALIDATION.md` are real files, and every later Phase 8 plan can reference them directly in `<verify>`.
</success_criteria>

<output>
After completion, create `.planning/phases/08-estados-responsividade-e-jornada-completa/08-00-SUMMARY.md`
</output>
