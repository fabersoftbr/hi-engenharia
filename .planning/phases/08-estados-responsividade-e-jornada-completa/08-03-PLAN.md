---
phase: 08-estados-responsividade-e-jornada-completa
plan: 03
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - apps/web/app/(platform)/layout.tsx
  - apps/web/components/platform/app-header.tsx
  - apps/web/components/platform/app-sidebar.tsx
  - apps/web/components/platform/app-breadcrumbs.tsx
  - apps/web/components/platform/profile-switcher.tsx
  - apps/web/components/platform/navigation-transition.tsx
  - packages/ui/src/components/data-table.tsx
  - packages/ui/src/components/tooltip.tsx
  - packages/ui/src/hooks/use-mobile.ts
  - apps/web/components/platform/drive/drive-file-table.tsx
  - packages/ui/src/styles/globals.css
autonomous: true
requirements:
  - RESP-01
user_setup: []
must_haves:
  truths:
    - "Users can read and navigate the shell cleanly at the locked `md` breakpoint without cramped headers, overflowing breadcrumbs, or ambiguous theme controls."
    - "Users can move between screens with the locked fade and slide navigation feel and land back at the top of the next page instead of inheriting stale scroll position."
    - "Users can read table content on smaller screens without header and body drift or immediate tooltip flashes."
  artifacts:
    - path: "apps/web/components/platform/app-header.tsx"
      provides: "Normalized responsive header"
      contains: "h-16"
    - path: "apps/web/components/platform/profile-switcher.tsx"
      provides: "Explicit theme switcher options"
      contains: "Seguir sistema"
    - path: "packages/ui/src/components/data-table.tsx"
      provides: "Shared responsive table contract"
      contains: "columnVisibility"
    - path: "apps/web/components/platform/navigation-transition.tsx"
      provides: "Fade and slide navigation transition plus scroll reset"
      contains: "window.scrollTo"
  key_links:
    - from: "apps/web/components/platform/app-header.tsx"
      to: "packages/ui/src/hooks/use-mobile.ts"
      via: "locked md breakpoint behavior"
      pattern: "useIsMobile"
    - from: "packages/ui/src/components/data-table.tsx"
      to: "apps/web/components/platform/projects/projects-list-page.tsx"
      via: "shared responsive table contract"
      pattern: "columnVisibility"
    - from: "packages/ui/src/components/tooltip.tsx"
      to: "apps/web/components/platform/app-sidebar.tsx"
      via: "collapsed navigation tooltips"
      pattern: "delayDuration"
---

<objective>
Normalize shell, header, theme, navigation, and table responsiveness around one shared `md` breakpoint contract.

Purpose: Close the responsive shell and table checker gaps without scattering one-off fixes through every screen.
Output: A normalized `h-16` header, sidebar and breadcrumb behavior that stays readable on mobile, a profile menu with theme options, a shared navigation transition layer, and a shared responsive table contract proven on the custom Drive table.
</objective>

<execution_context>
@D:/Development/Fabersoft/hi-engenharia/code/.codex/get-shit-done/workflows/execute-plan.md
@D:/Development/Fabersoft/hi-engenharia/code/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-estados-responsividade-e-jornada-completa/08-CONTEXT.md
@.planning/phases/08-estados-responsividade-e-jornada-completa/08-RESEARCH.md
@.planning/phases/08-estados-responsividade-e-jornada-completa/08-VALIDATION.md
@packages/ui/src/components/data-table.tsx
@packages/ui/src/components/tooltip.tsx
@packages/ui/src/hooks/use-mobile.ts
@packages/ui/src/styles/globals.css
@.agents/skills/shadcn/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Normalize shell, header, breadcrumbs, and theme controls for desktop and mobile readability</name>
  <files>apps/web/app/(platform)/layout.tsx, apps/web/components/platform/app-header.tsx, apps/web/components/platform/app-sidebar.tsx, apps/web/components/platform/app-breadcrumbs.tsx, apps/web/components/platform/profile-switcher.tsx, apps/web/components/platform/navigation-transition.tsx, packages/ui/src/components/tooltip.tsx, packages/ui/src/hooks/use-mobile.ts, packages/ui/src/styles/globals.css</files>
  <read_first>.planning/phases/08-estados-responsividade-e-jornada-completa/08-CONTEXT.md, .planning/phases/08-estados-responsividade-e-jornada-completa/08-RESEARCH.md, .agents/skills/shadcn/SKILL.md, apps/web/app/(platform)/layout.tsx, apps/web/components/platform/app-header.tsx, apps/web/components/platform/app-sidebar.tsx, apps/web/components/platform/app-breadcrumbs.tsx, apps/web/components/platform/profile-switcher.tsx, packages/ui/src/styles/globals.css</read_first>
  <acceptance_criteria>
- `rg -n "h-16" apps/web/components/platform/app-header.tsx` returns a match.
- `rg -n "256|64|w-64|w-16" apps/web/components/platform/app-sidebar.tsx` returns matches.
- `rg -n "Seguir sistema|Tema claro|Tema escuro" apps/web/components/platform/profile-switcher.tsx` returns matches.
- `rg -n "delayDuration|300" packages/ui/src/components/tooltip.tsx` returns matches.
- `rg -n "md" packages/ui/src/hooks/use-mobile.ts` returns a match.
- `rg -n "NavigationTransition|window.scrollTo|pathname" apps/web/app/(platform)/layout.tsx apps/web/components/platform/navigation-transition.tsx` returns matches.
- `rg -n "fade|slide|@keyframes" apps/web/components/platform/navigation-transition.tsx packages/ui/src/styles/globals.css` returns matches.
  </acceptance_criteria>
  <action>Use the repo-mandated shadcn-first/docs workflow before touching these frontend files: invoke the `shadcn` skill, run `pnpm dlx shadcn@latest info --json`, and fetch docs for `sidebar`, `breadcrumb`, `dropdown-menu`, `tooltip`, and `sheet` through `pnpm dlx shadcn@latest docs sidebar breadcrumb dropdown-menu tooltip sheet`. Normalize the shell to the locked Phase 8 measurements: the header must settle at `h-16`, the sidebar must preserve the locked 256px expanded and 64px collapsed widths, the breadcrumb row must wrap cleanly instead of overflowing, and the profile switcher must expose `Theme claro`, `Theme escuro`, and `Seguir sistema` alongside the existing `D` hotkey. Add explicit coverage for the locked `Fade/slide transition` and `Reset ao topo ao navegar para nova página` behaviors by introducing a single platform navigation-transition surface mounted in the platform layout and the supporting keyframes or utility classes in shared styles. Update the shared tooltip provider to the locked 300ms show delay so collapsed navigation and truncated text do not flash immediately. Keep the breakpoint contract tied to `md` through the shared `useIsMobile()` hook instead of screen-specific constants.</action>
  <verify>
    <automated>pnpm --filter web lint && pnpm --filter web typecheck && pnpm --dir apps/web vitest run components/platform/responsive/__tests__/responsive-layouts.test.tsx</automated>
  </verify>
  <done>The shell, navigation, breadcrumbs, theme controls, transitions, and scroll-reset behavior respect the locked Phase 8 responsive and interaction rules.</done>
</task>

<task type="auto">
  <name>Task 2: Move table responsiveness to one shared contract without taking ownership of list-page adoption</name>
  <files>packages/ui/src/components/data-table.tsx, apps/web/components/platform/drive/drive-file-table.tsx</files>
  <read_first>.planning/phases/08-estados-responsividade-e-jornada-completa/08-CONTEXT.md, .planning/phases/08-estados-responsividade-e-jornada-completa/08-RESEARCH.md, .agents/skills/shadcn/SKILL.md, packages/ui/src/components/data-table.tsx, apps/web/components/platform/drive/drive-file-table.tsx</read_first>
  <acceptance_criteria>
- `rg -n "columnVisibility|onColumnVisibilityChange" packages/ui/src/components/data-table.tsx` returns matches.
- `rg -n "hidden md:table-cell|columnVisibility" apps/web/components/platform/drive/drive-file-table.tsx` returns matches.
- `rg -n "\\+N|Tooltip" packages/ui/src/components/data-table.tsx` returns matches.
  </acceptance_criteria>
  <action>Use the same shadcn-first/docs workflow here before writing code. Refactor `packages/ui/src/components/data-table.tsx` so responsive behavior is driven by shared state rather than CSS-only hidden spans. Add a `columnVisibility` contract that consumers can control from `useIsMobile()`, and keep badge overflow aligned with the locked `3 badges + +N tooltip` rule. Keep this plan scoped to the shared table contract and the custom Drive table so list-page adoption remains owned by `08-02`. Because the Drive table is custom rather than the shared `DataTable`, update it in the same pass so its hidden columns and overflow behavior stay consistent with the new contract instead of drifting into a parallel implementation.</action>
  <verify>
    <automated>pnpm --filter web lint && pnpm --filter web typecheck && pnpm --dir apps/web vitest run components/platform/responsive/__tests__/responsive-layouts.test.tsx</automated>
  </verify>
  <done>Responsive table behavior now lives behind one shared contract and is visibly applied to the custom Drive table, ready for list-page adoption in `08-02`.</done>
</task>

</tasks>

<verification>
Run `pnpm --filter web lint && pnpm --filter web typecheck && pnpm --dir apps/web vitest run components/platform/responsive/__tests__/responsive-layouts.test.tsx`.
</verification>

<success_criteria>
The shell reads cleanly at the locked `md` breakpoint, the theme switcher exposes the required options, the locked transition and scroll-reset behavior is present, and responsive table behavior is centralized behind one shared contract.
</success_criteria>

<output>
After completion, create `.planning/phases/08-estados-responsividade-e-jornada-completa/08-03-SUMMARY.md`
</output>
