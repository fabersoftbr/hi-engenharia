---
phase: 08-estados-responsividade-e-jornada-completa
plan: 01
type: execute
wave: 1
depends_on:
  - 08-00
files_modified:
  - packages/ui/package.json
  - packages/ui/src/components/empty.tsx
  - packages/ui/src/components/empty-state.tsx
  - packages/ui/src/components/table-skeleton.tsx
  - packages/ui/src/components/card-grid-skeleton.tsx
  - packages/ui/src/components/pipeline-skeleton.tsx
  - packages/ui/src/components/detail-skeleton.tsx
  - packages/ui/src/components/sonner.tsx
  - apps/web/lib/feedback.ts
  - apps/web/hooks/use-simulated-loading.ts
  - apps/web/app/global-error.tsx
  - apps/web/app/(platform)/error.tsx
  - apps/web/app/erro/page.tsx
autonomous: true
requirements:
  - STAT-01
  - STAT-02
  - STAT-03
user_setup: []
must_haves:
  truths:
    - "Users can encounter one consistent visual language for loading, empty, and no-results states instead of screen-specific variants once Phase 8 adoption starts."
    - "Users receive consistent success, info, error, undo, and PDF-ready feedback instead of route-specific alert behavior."
    - "Users can recover from failures through framework fallbacks and a friendly `/erro` page with the locked illustration, retry, and home actions."
  artifacts:
    - path: "packages/ui/src/components/empty-state.tsx"
      provides: "Shared wrapper for empty and no-results surfaces"
      contains: "export interface EmptyStateProps"
    - path: "apps/web/lib/feedback.ts"
      provides: "Centralized Sonner helper API"
      contains: "showSuccessToast"
    - path: "apps/web/hooks/use-simulated-loading.ts"
      provides: "Development-only loading delay helper"
      contains: "useSimulatedLoading"
    - path: "apps/web/app/erro/page.tsx"
      provides: "Friendly error route with illustration"
      contains: "Tentar novamente"
  key_links:
    - from: "packages/ui/src/components/empty-state.tsx"
      to: "packages/ui/src/components/empty.tsx"
      via: "shadcn-first composition"
      pattern: "Empty"
    - from: "apps/web/lib/feedback.ts"
      to: "packages/ui/src/components/sonner.tsx"
      via: "shared toaster defaults"
      pattern: "bottom-right"
    - from: "apps/web/app/global-error.tsx"
      to: "apps/web/app/erro/page.tsx"
      via: "framework fallback and friendly route pairing"
      pattern: "Tentar novamente"
---

<objective>
Build the shared UI and feedback foundations that the rest of Phase 8 will adopt.

Purpose: Fix the checker blocker that stopped at primitives only by first creating reusable loading, empty, and feedback contracts, plus the required error surfaces, so later plans can focus on visible adoption rather than inventing infrastructure.
Output: Shared empty and skeleton components, a centralized feedback helper, a development-only simulated loading hook, and the `/erro` plus error-boundary surfaces.
</objective>

<execution_context>
@D:/Development/Fabersoft/hi-engenharia/code/.codex/get-shit-done/workflows/execute-plan.md
@D:/Development/Fabersoft/hi-engenharia/code/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-estados-responsividade-e-jornada-completa/08-CONTEXT.md
@.planning/phases/08-estados-responsividade-e-jornada-completa/08-RESEARCH.md
@.planning/phases/08-estados-responsividade-e-jornada-completa/08-VALIDATION.md
@packages/ui/package.json
@packages/ui/src/components/skeleton.tsx
@packages/ui/src/components/sonner.tsx
@.agents/skills/shadcn/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the shadcn-first empty and skeleton primitives that later plans must visibly adopt</name>
  <files>packages/ui/package.json, packages/ui/src/components/empty.tsx, packages/ui/src/components/empty-state.tsx, packages/ui/src/components/table-skeleton.tsx, packages/ui/src/components/card-grid-skeleton.tsx, packages/ui/src/components/pipeline-skeleton.tsx, packages/ui/src/components/detail-skeleton.tsx</files>
  <read_first>.planning/phases/08-estados-responsividade-e-jornada-completa/08-CONTEXT.md, .planning/phases/08-estados-responsividade-e-jornada-completa/08-RESEARCH.md, .agents/skills/shadcn/SKILL.md, packages/ui/package.json, packages/ui/src/components/skeleton.tsx, packages/ui/src/components/card.tsx</read_first>
  <acceptance_criteria>
- `rg -n "export function Empty|EmptyTitle|EmptyDescription|EmptyContent" packages/ui/src/components/empty.tsx` returns matches.
- `rg -n "export interface EmptyStateProps|icon:|title:|description:|action:" packages/ui/src/components/empty-state.tsx` returns matches.
- `rg -n "export function TableSkeleton|rowCount = 5|columnCount = 6" packages/ui/src/components/table-skeleton.tsx` returns matches.
- `rg -n "export function CardGridSkeleton|itemCount = 4" packages/ui/src/components/card-grid-skeleton.tsx` returns matches.
- `rg -n "export function PipelineSkeleton|columnCount = 4|cardsPerColumn = 3" packages/ui/src/components/pipeline-skeleton.tsx` returns matches.
- `rg -n "export function DetailSkeleton|sectionCount = 4" packages/ui/src/components/detail-skeleton.tsx` returns matches.
  </acceptance_criteria>
  <action>Start with the repo-mandated frontend workflow before writing code: invoke the `shadcn` skill, run `pnpm dlx shadcn@latest info --json`, and fetch docs for `empty` and `skeleton` through `pnpm dlx shadcn@latest docs empty skeleton` so the implementation stays inside the approved shadcn patterns. Add the missing `empty` primitive to `packages/ui` instead of hand-rolling empty markup in `apps/web`. Then create `EmptyState` as the only shared wrapper for empty and no-results screens with the exact prop surface `icon`, `title`, `description`, `action`, and `className`. Create exactly four named skeletons using the shared `Skeleton` primitive only: `TableSkeleton({ rowCount = 5, columnCount = 6 })`, `CardGridSkeleton({ itemCount = 4 })`, `PipelineSkeleton({ columnCount = 4, cardsPerColumn = 3 })`, and `DetailSkeleton({ sectionCount = 4 })`. Keep the shapes aligned with the existing dashboard, list, pipeline, and detail layouts so later adoption plans can drop them in without bespoke loading markup.</action>
  <verify>
    <automated>pnpm --filter @workspace/ui typecheck && pnpm --dir apps/web vitest run components/platform/states/__tests__/loading-and-feedback.test.tsx components/platform/states/__tests__/empty-states.test.tsx</automated>
  </verify>
  <done>The shared UI package now exposes the locked empty and skeleton primitives that later Phase 8 plans can adopt visibly across real screens.</done>
</task>

<task type="auto">
  <name>Task 2: Centralize feedback, simulated loading, and the locked error surfaces including `/erro` illustration</name>
  <files>packages/ui/src/components/sonner.tsx, apps/web/lib/feedback.ts, apps/web/hooks/use-simulated-loading.ts, apps/web/app/global-error.tsx, apps/web/app/(platform)/error.tsx, apps/web/app/erro/page.tsx</files>
  <read_first>.planning/phases/08-estados-responsividade-e-jornada-completa/08-CONTEXT.md, .planning/phases/08-estados-responsividade-e-jornada-completa/08-RESEARCH.md, packages/ui/src/components/sonner.tsx, apps/web/app/(platform)/layout.tsx, .agents/skills/shadcn/SKILL.md</read_first>
  <acceptance_criteria>
- `rg -n "bottom-right|visibleToasts|duration" packages/ui/src/components/sonner.tsx` returns matches.
- `rg -n "showSuccessToast|showInfoToast|showErrorToast|showUndoToast|showPdfReadyToast" apps/web/lib/feedback.ts` returns matches.
- `rg -n "useSimulatedLoading|developmentOnly|delayMs" apps/web/hooks/use-simulated-loading.ts` returns matches.
- `rg -n "reset\\(|Tentar novamente" apps/web/app/global-error.tsx apps/web/app/(platform)/error.tsx apps/web/app/erro/page.tsx` returns matches.
- `rg -n "ilustra" apps/web/app/erro/page.tsx` returns a match.
- `rg -n "Voltar ao início|Tentar novamente" apps/web/app/erro/page.tsx` returns matches.
  </acceptance_criteria>
  <action>Use the repo-mandated shadcn-first/docs workflow again before editing frontend code: invoke the `shadcn` skill, refresh project context with `pnpm dlx shadcn@latest info --json`, and fetch docs for `sonner`, `alert-dialog`, and `button` through `pnpm dlx shadcn@latest docs sonner alert-dialog button`. Update the shared toaster so it enforces the locked defaults from Phase 8: bottom-right placement, stacked toasts, and default durations that the helper layer can override to 3s success, 4s info, and 5s error. Create `apps/web/lib/feedback.ts` with the exact exported helpers `showSuccessToast`, `showInfoToast`, `showErrorToast`, `showUndoToast`, and `showPdfReadyToast`. Add `apps/web/hooks/use-simulated-loading.ts` as the only development-only loading-delay path for visible skeleton adoption in later plans. Then implement the required error surfaces: `app/global-error.tsx` for root failures, `app/(platform)/error.tsx` for segment failures, and `app/erro/page.tsx` as the friendly route with the locked illustration requirement carried into the implementation text. The `/erro` page must explicitly call for an illustration block, a friendly message, a `Tentar novamente` action, and a `Voltar ao início` link, not an icon-only fallback.</action>
  <verify>
    <automated>pnpm --filter web lint && pnpm --filter web typecheck && pnpm --dir apps/web vitest run components/platform/states/__tests__/loading-and-feedback.test.tsx</automated>
  </verify>
  <done>The app has one centralized feedback API, one simulated-loading helper, framework-native error boundaries, and a friendly `/erro` page with the required illustration and recovery actions.</done>
</task>

</tasks>

<verification>
Run `pnpm --filter @workspace/ui typecheck && pnpm --filter web lint && pnpm --filter web typecheck && pnpm --dir apps/web vitest run components/platform/states/__tests__/loading-and-feedback.test.tsx components/platform/states/__tests__/empty-states.test.tsx`.
</verification>

<success_criteria>
Phase 8 has the reusable state primitives and global feedback foundations it needs, including the development-only loading helper and the locked `/erro` illustration route, before screen-level adoption begins.
</success_criteria>

<output>
After completion, create `.planning/phases/08-estados-responsividade-e-jornada-completa/08-01-SUMMARY.md`
</output>
