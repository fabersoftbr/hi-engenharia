---
phase: 08-estados-responsividade-e-jornada-completa
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/ui/src/components/empty.tsx
  - packages/ui/src/components/alert-dialog.tsx
  - packages/ui/src/components/tabs.tsx
  - packages/ui/src/components/empty-state.tsx
  - packages/ui/src/components/table-skeleton.tsx
  - packages/ui/src/components/card-grid-skeleton.tsx
  - packages/ui/src/components/pipeline-skeleton.tsx
  - packages/ui/src/components/detail-skeleton.tsx
  - packages/ui/src/components/tooltip.tsx
  - packages/ui/src/components/sonner.tsx
  - packages/ui/package.json
  - apps/web/lib/feedback.ts
  - apps/web/app/(platform)/layout.tsx
  - apps/web/app/global-error.tsx
  - apps/web/app/(platform)/error.tsx
  - apps/web/app/erro/page.tsx
autonomous: true
requirements:
  - STAT-01
  - STAT-02
  - STAT-03
user_setup: []
must_haves:
  truths:
    - "As telas implementadas passam a ter uma base compartilhada para loading, empty, success e error states."
    - "O shell da plataforma usa a mesma configuracao de tooltip e toast em todas as rotas."
    - "A aplicacao oferece fallback amigavel para erros esperados e erros nao tratados."
  artifacts:
    - path: "packages/ui/src/components/empty-state.tsx"
      provides: "Wrapper tipado de empty state com icon, title, description e action"
      contains: "export function EmptyState"
    - path: "packages/ui/src/components/table-skeleton.tsx"
      provides: "Skeleton padronizado para tabelas"
      contains: "export function TableSkeleton"
    - path: "apps/web/app/global-error.tsx"
      provides: "Fallback global do App Router"
      contains: "function GlobalError"
  key_links:
    - from: "apps/web/app/(platform)/layout.tsx"
      to: "packages/ui/src/components/tooltip.tsx"
      via: "TooltipProvider"
      pattern: "TooltipProvider"
    - from: "apps/web/app/(platform)/layout.tsx"
      to: "packages/ui/src/components/sonner.tsx"
      via: "Toaster"
      pattern: "<Toaster"
    - from: "apps/web/lib/feedback.ts"
      to: "apps/web/app/erro/page.tsx"
      via: "success and error affordances"
      pattern: "showErrorToast|showSuccessToast"
---

<objective>
Estabelecer a fundacao compartilhada de estados visuais e feedback global para a Fase 08.

Purpose: Eliminar markup duplicado de empty/loading/error e travar um contrato unico de toast, tooltip e error fallback antes de mexer nas telas modulares.
Output: Primitivas compartilhadas no `@workspace/ui`, helpers de feedback em `apps/web`, e superficies de erro amigaveis no App Router.
</objective>

<execution_context>
@D:/Development/Fabersoft/hi-engenharia/code/.codex/get-shit-done/workflows/execute-plan.md
@D:/Development/Fabersoft/hi-engenharia/code/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-estados-responsividade-e-jornada-completa/08-CONTEXT.md
@.planning/phases/08-estados-responsividade-e-jornada-completa/08-RESEARCH.md
@.planning/phases/08-estados-responsividade-e-jornada-completa/08-VALIDATION.md
@.planning/phases/01-shell-de-acesso-e-identidade/01-03-SUMMARY.md
@.planning/phases/03-solicitacoes-de-orcamento/03-03-SUMMARY.md
@.planning/phases/05-anteprojetos-propostas-e-pre-os/05-04-SUMMARY.md
@packages/ui/src/components/skeleton.tsx
@packages/ui/src/components/tooltip.tsx
@packages/ui/src/components/sonner.tsx
@apps/web/app/(platform)/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the missing shared state and confirmation primitives to `@workspace/ui`</name>
  <files>
packages/ui/src/components/empty.tsx
packages/ui/src/components/alert-dialog.tsx
packages/ui/src/components/tabs.tsx
packages/ui/src/components/empty-state.tsx
packages/ui/package.json
  </files>
  <read_first>
.planning/phases/08-estados-responsividade-e-jornada-completa/08-CONTEXT.md
.planning/phases/08-estados-responsividade-e-jornada-completa/08-RESEARCH.md
.claude/skills/shadcn/SKILL.md
packages/ui/package.json
packages/ui/src/components/card.tsx
packages/ui/src/components/button.tsx
packages/ui/src/components/skeleton.tsx
  </read_first>
  <acceptance_criteria>
- `rg -n "export \\{ Empty" packages/ui/src/components/empty.tsx` returns a match.
- `rg -n "AlertDialogAction|AlertDialogCancel|AlertDialogTitle" packages/ui/src/components/alert-dialog.tsx` returns matches.
- `rg -n "TabsList|TabsTrigger|TabsContent" packages/ui/src/components/tabs.tsx` returns matches.
- `rg -n "export interface EmptyStateProps|export function EmptyState" packages/ui/src/components/empty-state.tsx` returns matches.
- `rg -n "\"@radix-ui/react-alert-dialog\"|\"@radix-ui/react-tabs\"" packages/ui/package.json` returns matches if the shadcn additions required new dependencies.
  </acceptance_criteria>
  <action>Use the shadcn-first workflow from the local `shadcn` skill before writing code. Confirm the current APIs for `empty`, `alert-dialog`, and `tabs`, then add those primitives into `packages/ui` rather than `apps/web` so they remain importable from `@workspace/ui/components/*`. After the registry add, create `packages/ui/src/components/empty-state.tsx` as a typed wrapper with the exact prop surface `icon`, `title`, `description`, `action`, and `className`. The `icon` prop must accept a `lucide-react` component reference, the `action` slot must accept `React.ReactNode`, and the rendered layout must rely on semantic tokens and `gap-*` spacing only. Do not handcraft a second visual system for empty states outside this wrapper.</action>
  <verify>
    <automated>pnpm --filter @workspace/ui typecheck</automated>
  </verify>
  <done>Shared empty, confirmation, and tab primitives exist in `packages/ui`, and `EmptyState` is importable from `@workspace/ui/components/empty-state` with the locked `icon/title/description/action` API.</done>
</task>

<task type="auto">
  <name>Task 2: Create the typed skeleton suite with layout-faithful shapes</name>
  <files>
packages/ui/src/components/table-skeleton.tsx
packages/ui/src/components/card-grid-skeleton.tsx
packages/ui/src/components/pipeline-skeleton.tsx
packages/ui/src/components/detail-skeleton.tsx
  </files>
  <read_first>
.planning/phases/08-estados-responsividade-e-jornada-completa/08-CONTEXT.md
.planning/phases/08-estados-responsividade-e-jornada-completa/08-RESEARCH.md
packages/ui/src/components/skeleton.tsx
packages/ui/src/components/data-table.tsx
apps/web/components/platform/budget-requests/budget-request-detail-page.tsx
apps/web/components/platform/crm/crm-pipeline-board.tsx
apps/web/components/platform/portal-dashboard.tsx
  </read_first>
  <acceptance_criteria>
- `rg -n "export function TableSkeleton" packages/ui/src/components/table-skeleton.tsx` returns a match.
- `rg -n "rowCount = 5|columnCount = 6" packages/ui/src/components/table-skeleton.tsx` returns matches.
- `rg -n "export function CardGridSkeleton" packages/ui/src/components/card-grid-skeleton.tsx` returns a match.
- `rg -n "export function PipelineSkeleton" packages/ui/src/components/pipeline-skeleton.tsx` returns a match.
- `rg -n "columnCount = 4|cardsPerColumn = 3" packages/ui/src/components/pipeline-skeleton.tsx` returns matches.
- `rg -n "export function DetailSkeleton" packages/ui/src/components/detail-skeleton.tsx` returns a match.
  </acceptance_criteria>
  <action>Create the four typed skeleton components exactly named `TableSkeleton`, `CardGridSkeleton`, `PipelineSkeleton`, and `DetailSkeleton`. Reuse the existing `Skeleton` primitive only; do not introduce standalone `animate-pulse` divs. Match the current module shapes already present in the repo: table header plus body rows, dashboard-style card grids, kanban columns with stacked cards, and the two-column detail shell that collapses vertically on mobile. Use these defaults unless a strong local constraint forces otherwise: `TableSkeleton({ rowCount = 5, columnCount = 6 })`, `CardGridSkeleton({ itemCount = 4 })`, `PipelineSkeleton({ columnCount = 4, cardsPerColumn = 3 })`, and `DetailSkeleton({ sectionCount = 4 })`.</action>
  <verify>
    <automated>pnpm --filter @workspace/ui typecheck</automated>
  </verify>
  <done>The UI package exports four named skeletons that mirror the platform layouts closely enough to be reused by list, dashboard, pipeline, and detail screens without custom one-off loading markup.</done>
</task>

<task type="auto">
  <name>Task 3: Centralize toast, tooltip, and App Router error handling for the platform shell</name>
  <files>
packages/ui/src/components/tooltip.tsx
packages/ui/src/components/sonner.tsx
apps/web/lib/feedback.ts
apps/web/app/(platform)/layout.tsx
apps/web/app/global-error.tsx
apps/web/app/(platform)/error.tsx
apps/web/app/erro/page.tsx
  </files>
  <read_first>
.planning/phases/08-estados-responsividade-e-jornada-completa/08-CONTEXT.md
.planning/phases/08-estados-responsividade-e-jornada-completa/08-RESEARCH.md
packages/ui/src/components/tooltip.tsx
packages/ui/src/components/sonner.tsx
apps/web/app/(platform)/layout.tsx
apps/web/components/platform/proposals/proposal-detail-page.tsx
apps/web/components/platform/price-table/price-table-upload-page.tsx
  </read_first>
  <acceptance_criteria>
- `rg -n "delayDuration = 300" packages/ui/src/components/tooltip.tsx` returns a match.
- `rg -n "position=\\\"bottom-right\\\"|visibleToasts=\\{5\\}|expand" packages/ui/src/components/sonner.tsx` returns matches.
- `rg -n "showSuccessToast|showInfoToast|showErrorToast|showUndoToast|showPdfReadyToast" apps/web/lib/feedback.ts` returns matches.
- `rg -n "TooltipProvider delayDuration=\\{300\\}|<Toaster" 'apps/web/app/(platform)/layout.tsx'` returns matches.
- `rg -n "function GlobalError|Tentar novamente|Voltar ao inicio" apps/web/app/global-error.tsx` returns matches.
- `rg -n "export default function PlatformError|Tentar novamente|Voltar ao inicio" 'apps/web/app/(platform)/error.tsx'` returns matches.
- `rg -n "Nao foi possivel carregar esta tela|Voltar ao inicio" apps/web/app/erro/page.tsx` returns matches.
  </acceptance_criteria>
  <action>Update `packages/ui/src/components/tooltip.tsx` so the default delay is exactly `300ms`. Update `packages/ui/src/components/sonner.tsx` so the shared `Toaster` defaults to `position="bottom-right"`, `visibleToasts={5}`, and `expand`, while preserving semantic styling tokens. Create `apps/web/lib/feedback.ts` with the exact exported helpers `showSuccessToast`, `showInfoToast`, `showErrorToast`, `showUndoToast`, and `showPdfReadyToast`. These helpers must enforce the locked durations `3000ms` for success, `4000ms` for info, and `5000ms` for error, and the undo/download actions must render the exact labels `Desfazer` and `Baixar`. Then wire the shell in `apps/web/app/(platform)/layout.tsx` to use `TooltipProvider delayDuration={300}` and the shared `Toaster`, create `apps/web/app/global-error.tsx` and `apps/web/app/(platform)/error.tsx` using App Router conventions, and add `apps/web/app/erro/page.tsx` with friendly pt-BR copy and the exact CTAs `Tentar novamente` and `Voltar ao inicio`. Do not add a duplicate error route or inline toast configuration elsewhere.</action>
  <verify>
    <automated>pnpm --filter web lint && pnpm --filter web typecheck && pnpm --filter web build</automated>
  </verify>
  <done>The platform shell now exposes one shared feedback API, one tooltip timing configuration, one toast mount, and both route-level and root-level error fallbacks with friendly Portuguese copy.</done>
</task>

</tasks>

<verification>
Run `pnpm lint && pnpm typecheck && pnpm build` from the repo root after the last task.
</verification>

<success_criteria>
The UI package exposes reusable empty/skeleton primitives, the platform shell mounts centralized tooltip and toast behavior, and the app has friendly error fallbacks for `/erro`, `(platform)` segment failures, and root failures.
</success_criteria>

<output>
After completion, create `.planning/phases/08-estados-responsividade-e-jornada-completa/08-01-SUMMARY.md`
</output>
