---
phase: 02-dashboard-e-home-operacional
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - apps/web/components/platform/dashboard/dashboard-welcome.tsx
  - apps/web/components/platform/dashboard/dashboard-summary-grid.tsx
  - apps/web/components/platform/dashboard/dashboard-quick-actions.tsx
autonomous: true
requirements:
  - PORT-01
  - PORT-03
user_setup: []
must_haves:
  truths:
    - The top of the dashboard shows a profile-aware welcome message with a visible pending-total summary.
    - The summary grid shows one clickable card per visible module with mocked activity counts and a pending-state badge.
    - The quick-action strip renders profile-specific actions as outline buttons that navigate to stable module routes.
  artifacts:
    - apps/web/components/platform/dashboard/dashboard-welcome.tsx
    - apps/web/components/platform/dashboard/dashboard-summary-grid.tsx
    - apps/web/components/platform/dashboard/dashboard-quick-actions.tsx
  key_links:
    - `apps/web/components/platform/dashboard/dashboard-summary-grid.tsx` imports the shared stat type from `apps/web/lib/dashboard-data.ts`.
    - `apps/web/components/platform/dashboard/dashboard-summary-grid.tsx` uses `next/link` to navigate to each module route.
    - `apps/web/components/platform/dashboard/dashboard-quick-actions.tsx` renders `Button` with `variant="outline"` for every profile action.
---

<objective>
Construir a faixa superior do dashboard com leitura rapida da operacao e acoes principais.

Purpose: Entregar a parte mais importante do portal para o primeiro olhar do usuario: saudacao, cards de resumo e atalhos operacionais coerentes com o perfil ativo.
Output: Componentes finais de boas-vindas, grade de resumo e acoes rapidas ligados ao contrato de dados compartilhado.
</objective>

<execution_context>
@D:/Development/Fabersoft/hi-engenharia/code/.codex/get-shit-done/workflows/execute-plan.md
@D:/Development/Fabersoft/hi-engenharia/code/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-dashboard-e-home-operacional/02-CONTEXT.md
@.planning/phases/02-dashboard-e-home-operacional/02-RESEARCH.md
@.planning/phases/02-dashboard-e-home-operacional/02-VALIDATION.md
@apps/web/lib/platform-config.ts
@apps/web/lib/dashboard-data.ts
@apps/web/components/platform/portal-dashboard.tsx
@apps/web/components/platform/dashboard/dashboard-welcome.tsx
@apps/web/components/platform/dashboard/dashboard-summary-grid.tsx
@apps/web/components/platform/dashboard/dashboard-quick-actions.tsx
@packages/ui/src/components/card.tsx
@packages/ui/src/components/badge.tsx
@packages/ui/src/components/button.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the profile-aware welcome strip</name>
  <files>apps/web/components/platform/dashboard/dashboard-welcome.tsx</files>
  <read_first>apps/web/components/platform/dashboard/dashboard-welcome.tsx, apps/web/lib/dashboard-data.ts, .planning/phases/02-dashboard-e-home-operacional/02-CONTEXT.md, packages/ui/src/components/card.tsx, packages/ui/src/components/badge.tsx</read_first>
  <action>Replace the placeholder in `apps/web/components/platform/dashboard/dashboard-welcome.tsx` with the final welcome strip. Render the exact sentence pattern `Bom dia, {profileLabel}. Voce tem {totalPendingCount} pendencias abertas.` and a secondary line `Acompanhe os modulos mais importantes do dia.`. Use `Card` composition so the strip remains visually aligned with the rest of the dashboard, and render the current profile label in a `Badge`. Keep the component driven only by `profileLabel` and `totalPendingCount`; do not read the session or provider again inside this file.</action>
  <verify>rg -n "Bom dia|pendencias abertas|Acompanhe os modulos mais importantes do dia\\.|Badge" apps/web/components/platform/dashboard/dashboard-welcome.tsx</verify>
  <acceptance_criteria>
    - `apps/web/components/platform/dashboard/dashboard-welcome.tsx` contains `Bom dia`
    - `apps/web/components/platform/dashboard/dashboard-welcome.tsx` contains `pendencias abertas`
    - `apps/web/components/platform/dashboard/dashboard-welcome.tsx` contains the exact sentence `Acompanhe os modulos mais importantes do dia.`
    - `apps/web/components/platform/dashboard/dashboard-welcome.tsx` imports `Badge`
  </acceptance_criteria>
  <done>The dashboard opens with a profile-aware summary that immediately communicates pending workload.</done>
</task>

<task type="auto">
  <name>Task 2: Build the clickable module summary grid</name>
  <files>apps/web/components/platform/dashboard/dashboard-summary-grid.tsx</files>
  <read_first>apps/web/components/platform/dashboard/dashboard-summary-grid.tsx, apps/web/lib/dashboard-data.ts, apps/web/lib/platform-config.ts, packages/ui/src/components/card.tsx, packages/ui/src/components/badge.tsx</read_first>
  <action>Replace the placeholder in `apps/web/components/platform/dashboard/dashboard-summary-grid.tsx` with the final module-card grid. Render one card per module in the `modules` prop using `modules.map`. Each card must show the module icon, the module label, the exact metric string `{activeCount} itens ativos`, and a badge that shows `{pendingCount} pendentes` when `pendingCount > 0` or `Em dia` when `pendingCount === 0`. Make the whole card navigate to the module route with `next/link`. Use the exact responsive grid classes `grid gap-4 md:grid-cols-2 xl:grid-cols-4` so the layout collapses from 4 columns to 2 and then 1 on smaller screens.</action>
  <verify>rg -n "modules\\.map|itens ativos|pendentes|Em dia|md:grid-cols-2|xl:grid-cols-4|next/link" apps/web/components/platform/dashboard/dashboard-summary-grid.tsx</verify>
  <acceptance_criteria>
    - `apps/web/components/platform/dashboard/dashboard-summary-grid.tsx` contains `modules.map`
    - `apps/web/components/platform/dashboard/dashboard-summary-grid.tsx` contains the exact string `itens ativos`
    - `apps/web/components/platform/dashboard/dashboard-summary-grid.tsx` contains the exact strings `pendentes` and `Em dia`
    - `apps/web/components/platform/dashboard/dashboard-summary-grid.tsx` contains the exact classes `md:grid-cols-2` and `xl:grid-cols-4`
    - `apps/web/components/platform/dashboard/dashboard-summary-grid.tsx` imports `next/link`
  </acceptance_criteria>
  <done>The user can scan every visible module and open it directly from the dashboard card grid.</done>
</task>

<task type="auto">
  <name>Task 3: Render the profile-specific quick-actions strip with outline buttons</name>
  <files>apps/web/components/platform/dashboard/dashboard-quick-actions.tsx</files>
  <read_first>apps/web/components/platform/dashboard/dashboard-quick-actions.tsx, apps/web/lib/dashboard-data.ts, .agents/skills/shadcn/SKILL.md, packages/ui/src/components/button.tsx</read_first>
  <action>Replace the placeholder in `apps/web/components/platform/dashboard/dashboard-quick-actions.tsx` with the final quick-actions section. Render the title `Acoes Rapidas` and map over the `actions` prop. For each action, render a `Button` with `variant="outline"` and `asChild`, wrapping a `next/link` target to the exact `href` provided by the data contract. Keep the layout on a wrapping row using the exact class string `flex flex-wrap gap-3`. Each button must include the action icon and label so the actions stay scannable for the active profile.</action>
  <verify>rg -n "Acoes Rapidas|variant=\\\"outline\\\"|asChild|flex flex-wrap gap-3|next/link" apps/web/components/platform/dashboard/dashboard-quick-actions.tsx</verify>
  <acceptance_criteria>
    - `apps/web/components/platform/dashboard/dashboard-quick-actions.tsx` contains the exact title `Acoes Rapidas`
    - `apps/web/components/platform/dashboard/dashboard-quick-actions.tsx` contains `variant=\"outline\"`
    - `apps/web/components/platform/dashboard/dashboard-quick-actions.tsx` contains `asChild`
    - `apps/web/components/platform/dashboard/dashboard-quick-actions.tsx` contains the exact class string `flex flex-wrap gap-3`
    - `apps/web/components/platform/dashboard/dashboard-quick-actions.tsx` imports `next/link`
  </acceptance_criteria>
  <done>The dashboard exposes the most common profile-specific actions as consistent shadcn buttons.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm lint && pnpm typecheck && pnpm build`
- [ ] `rg -n "Bom dia|pendencias abertas" apps/web/components/platform/dashboard/dashboard-welcome.tsx`
- [ ] `rg -n "itens ativos|Em dia|pendentes" apps/web/components/platform/dashboard/dashboard-summary-grid.tsx`
- [ ] `rg -n "Acoes Rapidas|variant=\"outline\"" apps/web/components/platform/dashboard/dashboard-quick-actions.tsx`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The dashboard top section communicates profile context, module status, and shortcut actions at a glance
  </success_criteria>

<output>
After completion, create `.planning/phases/02-dashboard-e-home-operacional/02-02-SUMMARY.md`
</output>
