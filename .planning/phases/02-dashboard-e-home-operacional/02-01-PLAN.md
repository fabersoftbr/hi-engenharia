---
phase: 02-dashboard-e-home-operacional
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/app/(platform)/portal/page.tsx
  - apps/web/components/platform/portal-dashboard.tsx
  - apps/web/components/platform/dashboard/dashboard-welcome.tsx
  - apps/web/components/platform/dashboard/dashboard-summary-grid.tsx
  - apps/web/components/platform/dashboard/dashboard-quick-actions.tsx
  - apps/web/components/platform/dashboard/dashboard-announcements.tsx
  - apps/web/components/platform/dashboard/dashboard-urgent-highlights.tsx
  - apps/web/lib/dashboard-data.ts
autonomous: true
requirements:
  - PORT-01
user_setup: []
must_haves:
  truths:
    - The final dashboard route lives at `/portal` inside the shared `(platform)` shell and no alternate `/dashboard` route is introduced.
    - The dashboard uses one shared mock-data contract for visible modules, quick actions, announcements, and urgent highlights.
    - Wave-2 plans can implement their sections without rewriting the route entrypoint because the composition boundaries already exist.
  artifacts:
    - apps/web/app/(platform)/portal/page.tsx
    - apps/web/components/platform/portal-dashboard.tsx
    - apps/web/lib/dashboard-data.ts
    - apps/web/components/platform/dashboard/dashboard-welcome.tsx
    - apps/web/components/platform/dashboard/dashboard-summary-grid.tsx
    - apps/web/components/platform/dashboard/dashboard-quick-actions.tsx
    - apps/web/components/platform/dashboard/dashboard-announcements.tsx
    - apps/web/components/platform/dashboard/dashboard-urgent-highlights.tsx
  key_links:
    - `apps/web/app/(platform)/portal/page.tsx` imports and renders `PortalDashboard` instead of `ModulePlaceholderPage`.
    - `apps/web/components/platform/portal-dashboard.tsx` composes `DashboardWelcome`, `DashboardSummaryGrid`, `DashboardQuickActions`, `DashboardAnnouncements`, and `DashboardUrgentHighlights` in the final section order.
    - `apps/web/lib/dashboard-data.ts` imports `getModulesForProfile`, `PROFILE_LABELS`, and `type ProfileKey` from `apps/web/lib/platform-config.ts` and exports the helpers consumed by `PortalDashboard`.
---

<objective>
Definir a base estrutural do dashboard antes da implementacao visual das secoes.

Purpose: Garantir que a rota `/portal`, o contrato de dados mockados e os limites entre componentes estejam estabelecidos uma vez, sem duplicar logica de perfil ou abrir conflito entre os planos da wave 2.
Output: Entrypoint final do portal, componente `PortalDashboard`, contrato compartilhado em `dashboard-data.ts` e arquivos de secao com interfaces estaveis.
</objective>

<execution_context>
@D:/Development/Fabersoft/hi-engenharia/code/.codex/get-shit-done/workflows/execute-plan.md
@D:/Development/Fabersoft/hi-engenharia/code/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-dashboard-e-home-operacional/02-CONTEXT.md
@.planning/phases/02-dashboard-e-home-operacional/02-RESEARCH.md
@.planning/phases/02-dashboard-e-home-operacional/02-VALIDATION.md
@.planning/phases/01-shell-de-acesso-e-identidade/01-CONTEXT.md
@apps/web/lib/platform-config.ts
@apps/web/lib/mock-session.ts
@packages/ui/src/styles/globals.css
@packages/ui/src/components/card.tsx
@packages/ui/src/components/button.tsx
@packages/ui/src/components/badge.tsx
@packages/ui/src/components/separator.tsx
@packages/ui/src/components/skeleton.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the shared dashboard mock-data contract from the platform config</name>
  <files>apps/web/lib/dashboard-data.ts</files>
  <read_first>.planning/phases/02-dashboard-e-home-operacional/02-CONTEXT.md, .planning/phases/02-dashboard-e-home-operacional/02-RESEARCH.md, apps/web/lib/platform-config.ts</read_first>
  <action>Create `apps/web/lib/dashboard-data.ts` with typed exports for `DashboardModuleStat`, `DashboardQuickAction`, `DashboardAnnouncement`, and `DashboardHighlight`. Import `getModulesForProfile`, `PROFILE_LABELS`, and `type ProfileKey` from `apps/web/lib/platform-config.ts`. Export these exact helpers and constants: `getDashboardModulesForProfile(profile)`, `getDashboardQuickActionsForProfile(profile)`, `getDashboardAnnouncements()`, `getDashboardHighlightsForProfile(profile)`, `getTotalPendingCount(modules)`, `DASHBOARD_ANNOUNCEMENTS`, and `DASHBOARD_HIGHLIGHTS`. `getDashboardModulesForProfile(profile)` must return `getModulesForProfile(profile)` filtered to exclude the `portal` module. Use these exact quick-action labels and targets: admin -> `Nova solicitacao de orcamento` `/orcamentos`, `Nova oportunidade` `/crm`, `Novo anteprojeto` `/anteprojetos`, `Novo comunicado` `/comunicacao`, `Abrir tabela de precos` `/tabela-de-precos`; commercial -> `Nova solicitacao de orcamento` `/orcamentos`, `Nova oportunidade` `/crm`, `Montar proposta` `/propostas`, `Abrir tabela de precos` `/tabela-de-precos`; partner -> `Nova solicitacao de orcamento` `/orcamentos`, `Ver meus orcamentos` `/orcamentos`; operations -> `Novo anteprojeto` `/anteprojetos`, `Abrir projetos` `/projetos`, `Acompanhar obras` `/obras`, `Abrir drive` `/drive`. Set `DASHBOARD_ANNOUNCEMENTS` to include these exact titles: `Planejamento comercial da semana`, `Janela de manutencao do portal`, `Atualizacao da tabela de precos`, and `Ritmo de aprovacao das obras`. Set `DASHBOARD_HIGHLIGHTS` to include items with these exact module labels: `Orcamentos`, `CRM`, `Anteprojetos`, and `Obras`.</action>
  <verify>rg -n "getDashboardModulesForProfile|getDashboardQuickActionsForProfile|getDashboardAnnouncements|getDashboardHighlightsForProfile|getTotalPendingCount|DASHBOARD_ANNOUNCEMENTS|DASHBOARD_HIGHLIGHTS|Planejamento comercial da semana|Janela de manutencao do portal|Atualizacao da tabela de precos|Ritmo de aprovacao das obras|Ver meus orcamentos|Acompanhar obras" apps/web/lib/dashboard-data.ts</verify>
  <acceptance_criteria>
    - `apps/web/lib/dashboard-data.ts` contains `export function getDashboardModulesForProfile`
    - `apps/web/lib/dashboard-data.ts` contains `export function getDashboardQuickActionsForProfile`, `export function getDashboardAnnouncements`, `export function getDashboardHighlightsForProfile`, and `export function getTotalPendingCount`
    - `apps/web/lib/dashboard-data.ts` imports `getModulesForProfile`, `PROFILE_LABELS`, and `type ProfileKey`
    - `apps/web/lib/dashboard-data.ts` contains the exact quick-action labels `Nova solicitacao de orcamento`, `Nova oportunidade`, `Montar proposta`, `Ver meus orcamentos`, `Acompanhar obras`, and `Abrir drive`
    - `apps/web/lib/dashboard-data.ts` contains `DASHBOARD_ANNOUNCEMENTS`, `DASHBOARD_HIGHLIGHTS`, and the exact announcement title `Planejamento comercial da semana`
  </acceptance_criteria>
  <done>The dashboard has one authoritative data contract for cards, actions, announcements, and highlights.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the portal placeholder with the final dashboard route entrypoint</name>
  <files>apps/web/app/(platform)/portal/page.tsx, apps/web/components/platform/portal-dashboard.tsx</files>
  <read_first>.planning/phases/02-dashboard-e-home-operacional/02-RESEARCH.md, apps/web/lib/dashboard-data.ts, apps/web/lib/platform-config.ts</read_first>
  <action>Create or overwrite `apps/web/app/(platform)/portal/page.tsx` so the route renders `PortalDashboard` at the final `/portal` path. Remove any `ModulePlaceholderPage` usage from that file. Create `apps/web/components/platform/portal-dashboard.tsx` as the stable dashboard composition shell. It must place sections in this exact order: welcome strip, summary-card grid, quick-actions row, footer grid with announcements on the left and urgent highlights on the right. Use a responsive footer wrapper that stacks on mobile and becomes a five-column layout on desktop with announcements spanning three columns and highlights spanning two columns via the exact classes `lg:grid lg:grid-cols-5`, `lg:col-span-3`, and `lg:col-span-2`.</action>
  <verify>rg -n "PortalDashboard" "apps/web/app/(platform)/portal/page.tsx" && rg -n "DashboardWelcome|DashboardSummaryGrid|DashboardQuickActions|DashboardAnnouncements|DashboardUrgentHighlights|lg:grid-cols-5|lg:col-span-3|lg:col-span-2" apps/web/components/platform/portal-dashboard.tsx</verify>
  <acceptance_criteria>
    - `apps/web/app/(platform)/portal/page.tsx` contains `PortalDashboard`
    - `apps/web/app/(platform)/portal/page.tsx` does not contain `ModulePlaceholderPage`
    - `apps/web/components/platform/portal-dashboard.tsx` imports all five section components
    - `apps/web/components/platform/portal-dashboard.tsx` contains the exact class strings `lg:grid-cols-5`, `lg:col-span-3`, and `lg:col-span-2`
  </acceptance_criteria>
  <done>The dashboard route exists at its final path and the page assembly order is fixed before the visual plans start.</done>
</task>

<task type="auto">
  <name>Task 3: Create compile-safe section component boundaries with stable prop signatures</name>
  <files>apps/web/components/platform/dashboard/dashboard-welcome.tsx, apps/web/components/platform/dashboard/dashboard-summary-grid.tsx, apps/web/components/platform/dashboard/dashboard-quick-actions.tsx, apps/web/components/platform/dashboard/dashboard-announcements.tsx, apps/web/components/platform/dashboard/dashboard-urgent-highlights.tsx, apps/web/components/platform/portal-dashboard.tsx</files>
  <read_first>apps/web/components/platform/portal-dashboard.tsx, apps/web/lib/dashboard-data.ts, packages/ui/src/components/card.tsx, packages/ui/src/components/skeleton.tsx</read_first>
  <action>Create the five dashboard section files with these exact exported component names and prop signatures: `DashboardWelcome({ profileLabel, totalPendingCount })`, `DashboardSummaryGrid({ modules })`, `DashboardQuickActions({ actions })`, `DashboardAnnouncements({ items })`, and `DashboardUrgentHighlights({ items })`. Use the exported types from `apps/web/lib/dashboard-data.ts` for the collection props. Each file must render a compile-safe temporary section shell with these exact Portuguese section titles: `Visao geral`, `Resumo por modulo`, `Acoes Rapidas`, `Comunicados`, and `Destaques e pendencias`. Update `apps/web/components/platform/portal-dashboard.tsx` to import these files and feed them concrete props from the shared data helpers: `PROFILE_LABELS[activeProfile]`, `getDashboardModulesForProfile(activeProfile)`, `getDashboardQuickActionsForProfile(activeProfile)`, `getDashboardAnnouncements()`, `getDashboardHighlightsForProfile(activeProfile)`, and `getTotalPendingCount(modules)`.</action>
  <verify>rg -n "export function DashboardWelcome|export function DashboardSummaryGrid|export function DashboardQuickActions|export function DashboardAnnouncements|export function DashboardUrgentHighlights|Visao geral|Resumo por modulo|Acoes Rapidas|Comunicados|Destaques e pendencias" apps/web/components/platform/dashboard && rg -n "PROFILE_LABELS\\[activeProfile\\]|getDashboardModulesForProfile\\(activeProfile\\)|getDashboardQuickActionsForProfile\\(activeProfile\\)|getDashboardAnnouncements\\(\\)|getDashboardHighlightsForProfile\\(activeProfile\\)|getTotalPendingCount\\(modules\\)" apps/web/components/platform/portal-dashboard.tsx</verify>
  <acceptance_criteria>
    - `apps/web/components/platform/dashboard/dashboard-welcome.tsx` contains `export function DashboardWelcome`
    - `apps/web/components/platform/dashboard/dashboard-summary-grid.tsx` contains `export function DashboardSummaryGrid`
    - `apps/web/components/platform/dashboard/dashboard-quick-actions.tsx` contains `export function DashboardQuickActions`
    - `apps/web/components/platform/dashboard/dashboard-announcements.tsx` contains `export function DashboardAnnouncements`
    - `apps/web/components/platform/dashboard/dashboard-urgent-highlights.tsx` contains `export function DashboardUrgentHighlights`
    - `apps/web/components/platform/portal-dashboard.tsx` contains `getDashboardModulesForProfile(activeProfile)`, `getDashboardQuickActionsForProfile(activeProfile)`, and `getDashboardHighlightsForProfile(activeProfile)`
  </acceptance_criteria>
  <done>The wave-2 plans now have stable component targets and can implement their sections without changing the route contract.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm --filter web lint && pnpm --filter web typecheck`
- [ ] `rg -n "PortalDashboard" "apps/web/app/(platform)/portal/page.tsx"`
- [ ] `rg -n "DashboardWelcome|DashboardSummaryGrid|DashboardQuickActions|DashboardAnnouncements|DashboardUrgentHighlights" apps/web/components/platform/portal-dashboard.tsx`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The portal route, shared data contract, and section boundaries exist before the visual implementation plans start
  </success_criteria>

<output>
After completion, create `.planning/phases/02-dashboard-e-home-operacional/02-01-SUMMARY.md`
</output>
