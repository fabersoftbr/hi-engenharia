---
phase: 09
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/components/platform/budget-requests/budget-request-form.tsx
  - apps/web/components/platform/budget-requests/budget-request-status-dialog.tsx
  - apps/web/components/platform/proposals/proposal-builder-page.tsx
  - apps/web/components/platform/drive/drive-page.tsx
  - apps/web/components/brand-logo.tsx
autonomous: true
requirements:
  - CLEAN-02
must_haves:
  truths:
    - No console.log statements remain in production code (except console.error for critical errors)
    - No TODO comments without issue tracking references
    - No commented-out code blocks
  artifacts:
    - path: apps/web/components/platform/**/*.tsx
      provides: Components free of debug artifacts
  key_links:
    - from: apps/web/components/**/*.tsx
      to: browser console
      via: console.* calls
      pattern: "console\\.(log|debug|info|warn)"
---

<objective>
Remove debug artifacts (console.log, TODO comments without tracking, dead code) from all component files.

Purpose: Clean up leftover debug statements and placeholder TODOs that were added during development but should not remain in production code.
Output: Components free of console.log debug statements and unresolved TODOs.
</objective>

<execution_context>
@D:/Development/Fabersoft/hi-engenharia/code/.claude/get-shit-done/workflows/execute-plan.md
@D:/Development/Fabersoft/hi-engenharia/code/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-usar-a-skill-clean-code-e-fazer-uma-auditoria-completa-em-todos-os-arquivos-de-codigo-dessa-branch-para-aplicar-as-boas-praticas/09-RESEARCH.md
@.planning/phases/09-usar-a-skill-clean-code-e-fazer-uma-auditoria-completa-em-todos-os-arquivos-de-codigo-dessa-branch-para-aplicar-as-boas-praticas/09-VALIDATION.md

## Known console.log locations (from grep search):
- apps/web/components/platform/budget-requests/budget-request-form.tsx:39-40
- apps/web/components/platform/budget-requests/budget-request-status-dialog.tsx:44
- apps/web/components/platform/proposals/proposal-builder-page.tsx:135
- apps/web/components/platform/drive/drive-page.tsx:99, 104, 109
- apps/web/components/brand-logo.tsx:25 (TODO comment)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove console.log debug statements from components</name>
  <files>
    - apps/web/components/platform/budget-requests/budget-request-form.tsx
    - apps/web/components/platform/budget-requests/budget-request-status-dialog.tsx
    - apps/web/components/platform/proposals/proposal-builder-page.tsx
    - apps/web/components/platform/drive/drive-page.tsx
  </files>
  <read_first>
    - apps/web/components/platform/budget-requests/budget-request-form.tsx
    - apps/web/components/platform/budget-requests/budget-request-status-dialog.tsx
    - apps/web/components/platform/proposals/proposal-builder-page.tsx
    - apps/web/components/platform/drive/drive-page.tsx
  </read_first>
  <acceptance_criteria>
    - grep -r "console\.(log|debug|info)" apps/web/components/ returns no results
    - Functions that had console.log still work correctly (handlers remain functional)
    - Running `pnpm lint && pnpm typecheck` passes
  </acceptance_criteria>
  <action>
    Remove all console.log, console.debug, and console.info statements from component files.

    Known locations to fix:
    1. budget-request-form.tsx lines 39-40:
       ```typescript
       // BEFORE:
       console.log("Form submitted:", data)
       console.log("Preview request ID:", PREVIEW_SUBMITTED_REQUEST_ID)
       // AFTER: Remove these lines entirely (mock submit is intentional, no logging needed)
       ```

    2. budget-request-status-dialog.tsx line 44:
       - Remove the console.log call in the handler

    3. proposal-builder-page.tsx line 135:
       - Remove the console.log call, keep the toast.success for user feedback

    4. drive-page.tsx lines 99, 104, 109:
       - These are placeholder handlers. Remove console.log calls, keep empty handlers or add comments explaining they are mock/placeholder actions.

    For each removal:
    - Preserve the function signature and any actual logic
    - Remove only the console.* debug statements
    - Keep toast.success/error calls as they provide user feedback
  </action>
  <verify>
    <automated>grep -rn "console\\.(log|debug|info)" apps/web/components/ --include="*.tsx" --include="*.ts" || echo "No console.log/debug/info found"</automated>
  </verify>
  <done>All console.log, console.debug, and console.info statements removed from apps/web/components/. Lint and typecheck pass.</done>
</task>

<task type="auto">
  <name>Task 2: Handle TODO comments and commented-out code</name>
  <files>
    - apps/web/components/brand-logo.tsx
  </files>
  <read_first>
    - apps/web/components/brand-logo.tsx
  </read_first>
  <acceptance_criteria>
    - TODO comments either have issue references or are converted to regular comments explaining the situation
    - No large blocks of commented-out code (more than 3 lines)
    - Running `pnpm lint && pnpm typecheck` passes
  </acceptance_criteria>
  <action>
    Review and handle TODO comments in component files.

    Known location:
    1. brand-logo.tsx line 25:
       ```typescript
       // TODO: Replace with actual logo when files are provided
       ```

    Decision for this TODO:
    - This is a valid placeholder awaiting external assets (logo files)
    - Either add a note that this is tracked in the project backlog, or convert to a regular explanatory comment:
       ```typescript
       // Logo placeholder - text-only fallback until official logo files are provided
       ```

    Search for any other TODO/FIXME/HACK comments:
    ```bash
    grep -rn "TODO\|FIXME\|HACK\|XXX" apps/web/components/ --include="*.tsx" --include="*.ts"
    ```

    For each found:
    - If it's a temporary hack with a clear fix: create an issue reference or fix it
    - If it's awaiting external input (like logo files): convert to explanatory comment
    - If it's obsolete: remove the comment

    Also check for and remove any large blocks of commented-out code (dead code).
  </action>
  <verify>
    <automated>grep -rn "TODO\|FIXME\|HACK" apps/web/components/ --include="*.tsx" --include="*.ts" || echo "No TODO/FIXME/HACK found"</automated>
  </verify>
  <done>TODO comments handled appropriately (referenced, converted to notes, or removed). No dead commented code blocks remain.</done>
</task>

</tasks>

<verification>
After completing all tasks:
1. Run `pnpm lint && pnpm typecheck` - must pass
2. Verify no console.log/debug/info remain: `grep -r "console\.(log|debug|info)" apps/web/components/ --include="*.tsx"`
3. Verify TODOs are handled: `grep -r "TODO" apps/web/components/ --include="*.tsx"`
</verification>

<success_criteria>
- Zero console.log, console.debug, console.info statements in apps/web/components/
- All TODO comments either have issue references or are converted to explanatory notes
- No dead commented-out code blocks
- `pnpm lint && pnpm typecheck` passes
</success_criteria>

<output>
After completion, create `.planning/phases/09-usar-a-skill-clean-code-e-fazer-uma-auditoria-completa-em-todos-os-arquivos-de-codigo-dessa-branch-para-aplicar-as-boas-praticas/09-02-SUMMARY.md`
</output>
