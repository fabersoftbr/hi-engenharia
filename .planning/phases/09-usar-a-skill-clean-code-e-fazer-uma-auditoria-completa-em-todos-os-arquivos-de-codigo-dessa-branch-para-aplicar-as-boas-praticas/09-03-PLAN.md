---
phase: 09
plan: 03
type: execute
wave: 2
depends_on:
  - 09-01
  - 09-02
files_modified:
  - apps/web/components/platform/drive/drive-page.tsx
autonomous: true
requirements:
  - CLEAN-03
must_haves:
  truths:
    - No function exceeds 40 lines (excluding data arrays/constants)
    - Complex conditional logic is extracted to helper functions
    - Components follow single responsibility principle
    - No deeply nested conditionals (max 3 levels)
  artifacts:
    - path: apps/web/components/platform/drive/drive-page.tsx
      provides: Refactored DrivePage with extracted helpers
      max_function_lines: 40
  key_links:
    - from: helper functions
      to: renderContent
      via: function calls
      pattern: "function.*\\(.*\\).*{"
---

<objective>
Audit and refactor functions for length and complexity, extracting helpers where needed.

Purpose: Ensure functions follow Clean Code principles - single responsibility, reasonable length (under 40 lines), and clear naming.
Output: Components with extracted helper functions, no oversized render methods.
</objective>

<execution_context>
@D:/Development/Fabersoft/hi-engenharia/code/.claude/get-shit-done/workflows/execute-plan.md
@D:/Development/Fabersoft/hi-engenharia/code/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-usar-a-skill-clean-code-e-fazer-uma-auditoria-completa-em-todos-os-arquivos-de-codigo-dessa-branch-para-aplicar-as-boas-praticas/09-RESEARCH.md
@.planning/phases/09-usar-a-skill-clean-code-e-fazer-uma-auditoria-completa-em-todos-os-arquivos-de-codigo-dessa-branch-para-aplicar-as-boas-praticas/09-VALIDATION.md

## Reference: Function Length Guidelines
- Under 20 lines: Ideal
- 20-40 lines: Acceptable for complex logic
- Over 40 lines: Should be refactored (extract helpers)

## Known Long Function:
- drive-page.tsx renderContent(): ~130 lines - needs extraction
</context>

<tasks>

<task type="auto">
  <name>Task 1: Identify functions exceeding length threshold</name>
  <files>
    - apps/web/components/platform/**/*.tsx
  </files>
  <read_first>
    - apps/web/components/platform/drive/drive-page.tsx
    - apps/web/components/platform/crm/crm-workspace-page.tsx
    - apps/web/components/platform/anteprojects/anteprojects-workspace-page.tsx
    - apps/web/components/platform/proposals/proposal-builder-page.tsx
  </read_first>
  <acceptance_criteria>
    - List of functions exceeding 40 lines generated
    - Functions categorized by complexity (simple extraction vs. architectural refactoring)
  </acceptance_criteria>
  <action>
    Audit all component files for function length violations.

    Search strategy:
    1. Read the largest component files first (workspace pages, builder pages)
    2. For each function, count lines between opening { and closing }
    3. Flag functions over 40 lines for refactoring

    Priority files to audit (based on expected complexity):
    - drive-page.tsx (renderContent is known to be ~130 lines)
    - crm-workspace-page.tsx
    - anteprojects-workspace-page.tsx
    - proposal-builder-page.tsx

    For each oversized function, identify:
    - Function name and line count
    - Logical sections that could be extracted
    - Suggested helper function names

    Document findings in a structured list. DO NOT modify files yet.
  </action>
  <verify>
    <automated>echo "Manual audit - verify by reading identified files"</automated>
  </verify>
  <done>Audit report generated listing all functions over 40 lines with suggested extractions</done>
</task>

<task type="auto">
  <name>Task 2: Refactor drive-page.tsx renderContent function</name>
  <files>
    - apps/web/components/platform/drive/drive-page.tsx
  </files>
  <read_first>
    - apps/web/components/platform/drive/drive-page.tsx
  </read_first>
  <acceptance_criteria>
    - renderContent function is under 40 lines
    - Extracted helpers have clear, descriptive names
    - All extracted helpers are defined before renderContent
    - Running `pnpm lint && pnpm typecheck` passes
    - Visual behavior unchanged (manual verification recommended)
  </acceptance_criteria>
  <action>
    Refactor the renderContent function in drive-page.tsx by extracting logical sections into helper functions.

    Current structure analysis (from file read):
    The renderContent function handles multiple rendering scenarios:
    1. Search results view (with empty state)
    2. Inside folder with subfolder selected (placeholder)
    3. Inside folder showing subfolders (cards + placeholder)
    4. Root view with tabs

    Suggested extraction pattern:
    ```typescript
    // Extract these helpers BEFORE the component:

    function renderSearchResults(
      searchResults: SearchResultType,
      onFolderClick: (id: string) => void,
      onFolderAction: (id: string, action: string) => void
    ): React.ReactNode {
      // Search results rendering logic
    }

    function renderFolderWithSubfolders(
      folder: DriveFolder,
      onSubfolderClick: (id: string) => void
    ): React.ReactNode {
      // Subfolder cards rendering
    }

    function renderTabsView(
      currentSection: DriveSection,
      visibleTabs: DriveSection[],
      onTabChange: (value: string) => void,
      onFolderClick: (id: string) => void,
      onFolderAction: (id: string, action: string) => void,
      onUpload: () => void
    ): React.ReactNode {
      // Tabs with folder lists
    }
    ```

    After extraction, renderContent should be a simple dispatcher:
    ```typescript
    const renderContent = () => {
      if (searchResults) {
        return renderSearchResults(searchResults, handleFolderClick, handleFolderAction)
      }

      if (currentFolderId && currentSubfolderId) {
        return <FileListPlaceholder />
      }

      if (currentFolderId && currentFolder) {
        return renderFolderWithSubfolders(currentFolder, handleSubfolderClick)
      }

      return renderTabsView(currentSection, visibleTabs, handleTabChange, handleFolderClick, handleFolderAction, handleUpload)
    }
    ```

    Run `pnpm lint && pnpm typecheck` after refactoring to verify no regressions.
  </action>
  <verify>
    <automated>pnpm lint && pnpm typecheck</automated>
  </verify>
  <done>renderContent refactored to under 40 lines with extracted helper functions. All tests pass.</done>
</task>

</tasks>

<verification>
After completing all tasks:
1. Run `pnpm lint && pnpm typecheck` - must pass
2. Verify renderContent is under 40 lines by manual inspection
3. Verify all extracted helpers have clear names and single responsibilities
</verification>

<success_criteria>
- drive-page.tsx renderContent function refactored to under 40 lines
- Helper functions extracted with clear, descriptive names
- All components follow single responsibility principle
- `pnpm lint && pnpm typecheck` passes
</success_criteria>

<output>
After completion, create `.planning/phases/09-usar-a-skill-clean-code-e-fazer-uma-auditoria-completa-em-todos-os-arquivos-de-codigo-dessa-branch-para-aplicar-as-boas-praticas/09-03-SUMMARY.md`
</output>
