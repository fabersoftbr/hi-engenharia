---
phase: 06-projetos-e-pipeline-de-obras
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - packages/ui/package.json
  - packages/ui/src/components/progress.tsx
  - apps/web/app/(platform)/projetos/[projectId]/page.tsx
  - apps/web/components/platform/app-breadcrumbs.tsx
  - apps/web/components/platform/projects/project-detail-page.tsx
  - apps/web/components/platform/projects/project-stage-history.tsx
  - apps/web/components/platform/projects/project-stage-change-select.tsx
  - apps/web/lib/projects-data.ts
autonomous: true
requirements:
  - PROJ-02
user_setup: []
must_haves:
  truths:
    - `/projetos/[projectId]` renders a real project detail page from the seeded projects data contract and calls `notFound()` for missing ids.
    - The detail screen shows the locked two-column layout with summary data, linked proposal and anteprojeto context, attachment actions, current stage, and progress.
    - Changing stage in the detail view updates local state, recalculates `progressPercent`, and appends a new history entry without introducing persistence or backend logic.
  artifacts:
    - packages/ui/package.json
    - packages/ui/src/components/progress.tsx
    - apps/web/app/(platform)/projetos/[projectId]/page.tsx
    - apps/web/components/platform/app-breadcrumbs.tsx
    - apps/web/components/platform/projects/project-detail-page.tsx
    - apps/web/components/platform/projects/project-stage-history.tsx
    - apps/web/components/platform/projects/project-stage-change-select.tsx
    - apps/web/lib/projects-data.ts
  key_links:
    - `apps/web/app/(platform)/projetos/[projectId]/page.tsx` resolves a record through `getProjectById(projectId)` and calls `notFound()` when missing.
    - `apps/web/components/platform/app-breadcrumbs.tsx` resolves `/projetos/{id}` to the seeded project title from `projects-data.ts`.
    - `apps/web/components/platform/projects/project-detail-page.tsx` renders both `ProjectStageHistory` and `ProjectStageChangeSelect` and uses the shared `Progress` component.
---

<objective>
Entregar a tela de detalhe do projeto com status, arquivos relacionados, historico e conexoes visuais com proposta e anteprojeto.

Purpose: Transformar o modulo em uma experiencia navegavel de projeto individual, com breadcrumb coerente, dados mockados completos e mudanca local de etapa.
Output: Rota dinamica `/projetos/[projectId]`, breadcrumb contextual, layout final do projeto, timeline visual, barra de progresso e seletor de etapa.
</objective>

<execution_context>
@D:/Development/Fabersoft/hi-engenharia/code/.codex/get-shit-done/workflows/execute-plan.md
@D:/Development/Fabersoft/hi-engenharia/code/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-projetos-e-pipeline-de-obras/06-CONTEXT.md
@.planning/phases/06-projetos-e-pipeline-de-obras/06-RESEARCH.md
@.planning/phases/06-projetos-e-pipeline-de-obras/06-VALIDATION.md
@.planning/phases/06-projetos-e-pipeline-de-obras/06-01-PLAN.md
@apps/web/lib/projects-data.ts
@apps/web/components/platform/app-breadcrumbs.tsx
@apps/web/components/platform/crm/crm-opportunity-detail-page.tsx
@apps/web/components/platform/anteprojects/anteproject-detail-page.tsx
@packages/ui/package.json
@packages/ui/src/components/card.tsx
@packages/ui/src/components/select.tsx
@packages/ui/src/components/button.tsx
@packages/ui/src/components/separator.tsx
@.agents/skills/shadcn/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the shared progress primitive, project detail route boundary, and dynamic breadcrumb title</name>
  <files>packages/ui/package.json, packages/ui/src/components/progress.tsx, apps/web/app/(platform)/projetos/[projectId]/page.tsx, apps/web/components/platform/app-breadcrumbs.tsx</files>
  <read_first>packages/ui/package.json, apps/web/components/platform/app-breadcrumbs.tsx, apps/web/lib/projects-data.ts, apps/web/app/(platform)/crm/[opportunityId]/page.tsx, .planning/phases/06-projetos-e-pipeline-de-obras/06-RESEARCH.md, .agents/skills/shadcn/SKILL.md</read_first>
  <action>Use the shadcn workflow to add the shared `progress` primitive into `packages/ui`, updating `packages/ui/package.json` and creating `packages/ui/src/components/progress.tsx` if the component is missing. Create `apps/web/app/(platform)/projetos/[projectId]/page.tsx` using the exact Next.js 16 signature `params: Promise<{ projectId: string }>`; resolve `projectId`, load the record through `getProjectById(projectId)`, and call `notFound()` when the record does not exist. Refactor `apps/web/components/platform/app-breadcrumbs.tsx` so it builds a breadcrumb items array instead of only two hardcoded slots, preserves the existing CRM, anteprojetos, propostas, and tabela-de-precos behaviors, and adds `/projetos/{dynamic-id}` support that resolves the second breadcrumb item to the exact seeded project title from `getProjectById(projectId)` with `Detalhe` as the fallback label when the id is missing from the seed data.</action>
  <verify>rg -n "\"@radix-ui/react-progress\"|progress" packages/ui/package.json packages/ui/src/components/progress.tsx && rg -n "params: Promise<\\{ projectId: string \\}>|getProjectById|notFound" "apps/web/app/(platform)/projetos/[projectId]/page.tsx" && rg -n "getProjectById|Detalhe|projetos" apps/web/components/platform/app-breadcrumbs.tsx</verify>
  <acceptance_criteria>
    - `packages/ui/src/components/progress.tsx` exists and exports the shared progress component
    - `apps/web/app/(platform)/projetos/[projectId]/page.tsx` contains `params: Promise<{ projectId: string }>`
    - `apps/web/app/(platform)/projetos/[projectId]/page.tsx` contains `getProjectById`
    - `apps/web/app/(platform)/projetos/[projectId]/page.tsx` contains `notFound`
    - `apps/web/components/platform/app-breadcrumbs.tsx` contains `getProjectById`
    - `apps/web/components/platform/app-breadcrumbs.tsx` keeps the exact label `Detalhe` as fallback behavior
  </acceptance_criteria>
  <done>The project detail route now has a stable App Router boundary and the shell can identify nested project pages by their seeded titles.</done>
</task>

<task type="auto">
  <name>Task 2: Build the project detail layout, linked context blocks, and attachment presentation</name>
  <files>apps/web/components/platform/projects/project-detail-page.tsx, apps/web/components/platform/projects/project-stage-history.tsx</files>
  <read_first>apps/web/components/platform/crm/crm-opportunity-detail-page.tsx, apps/web/components/platform/anteprojects/anteproject-detail-page.tsx, apps/web/lib/projects-data.ts, .planning/phases/06-projetos-e-pipeline-de-obras/06-CONTEXT.md, packages/ui/src/components/card.tsx, packages/ui/src/components/button.tsx, packages/ui/src/components/progress.tsx, packages/ui/src/components/separator.tsx</read_first>
  <action>Create `apps/web/components/platform/projects/project-stage-history.tsx` so it renders history rows from `ProjectHistoryEntry` with stage label, `changedAt`, and responsible name. Create `project-detail-page.tsx` with the exact layout class string `lg:grid lg:grid-cols-[minmax(0,1.2fr)_minmax(320px,0.8fr)] lg:gap-6`. The left column must render the exact headings and labels `Resumo do projeto`, `Cliente`, `Tipo`, `Potencia (kWp)`, `Local`, `Prazo previsto`, `Responsavel`, and `Arquivos relacionados`. The right column must render the exact headings `Status atual`, `Acoes`, and `Historico de etapas`. Render a progress block with the exact label `Progresso`, a next-stage line with the exact label `Proxima etapa`, linked context cards with the exact titles `Proposta` and `Anteprojeto` pointing to `/propostas/{proposalId}` and `/anteprojetos/{anteprojectId}` when those ids exist, and an action stack containing the exact visible actions `Ver anteprojeto` and `Gerar relatorio` alongside the stage-change control. Every attachment row must render the exact action label `Baixar arquivo` and stay inside the detail page instead of opening a new module route.</action>
  <verify>rg -n "Resumo do projeto|Cliente|Tipo|Potencia \\(kWp\\)|Local|Prazo previsto|Responsavel|Arquivos relacionados|Status atual|Acoes|Historico de etapas|Progresso|Proxima etapa|Proposta|Anteprojeto|Ver anteprojeto|Gerar relatorio|Baixar arquivo|/propostas/|/anteprojetos/|lg:grid lg:grid-cols-\\[minmax\\(0,1.2fr\\)_minmax\\(320px,0.8fr\\)\\]" apps/web/components/platform/projects/project-detail-page.tsx && rg -n "changedAt|changedBy|ProjectHistoryEntry" apps/web/components/platform/projects/project-stage-history.tsx</verify>
  <acceptance_criteria>
    - `apps/web/components/platform/projects/project-detail-page.tsx` contains the exact labels `Cliente`, `Tipo`, `Potencia (kWp)`, `Local`, `Prazo previsto`, and `Responsavel`
    - `apps/web/components/platform/projects/project-detail-page.tsx` contains the exact headings `Status atual`, `Acoes`, and `Historico de etapas`
    - `apps/web/components/platform/projects/project-detail-page.tsx` contains the exact labels `Progresso` and `Proxima etapa`
    - `apps/web/components/platform/projects/project-detail-page.tsx` contains the exact titles `Proposta` and `Anteprojeto`
    - `apps/web/components/platform/projects/project-detail-page.tsx` contains the exact actions `Ver anteprojeto` and `Gerar relatorio`
    - `apps/web/components/platform/projects/project-detail-page.tsx` contains the exact action `Baixar arquivo`
    - `apps/web/components/platform/projects/project-stage-history.tsx` contains `changedAt`
  </acceptance_criteria>
  <done>The detail screen now communicates project context, current stage, linked modules, and attachment access in the locked desktop/mobile structure.</done>
</task>

<task type="auto">
  <name>Task 3: Implement local stage changes, progress recalculation, and next-stage feedback</name>
  <files>apps/web/components/platform/projects/project-detail-page.tsx, apps/web/components/platform/projects/project-stage-change-select.tsx, apps/web/lib/projects-data.ts</files>
  <read_first>apps/web/components/platform/projects/project-detail-page.tsx, apps/web/lib/projects-data.ts, .planning/phases/06-projetos-e-pipeline-de-obras/06-CONTEXT.md, packages/ui/src/components/select.tsx, packages/ui/src/components/progress.tsx</read_first>
  <action>Create `apps/web/components/platform/projects/project-stage-change-select.tsx` with a shadcn `Select` that reads the exact options from `WORK_STAGE_ORDER` and uses the exact visible label `Mudar estagio`. Update `projects-data.ts` so `getProjectProgress(stageId)` returns the integer percentage for the current stage position across the 11 work stages. Update `ProjectDetailPage` so it owns local project state, passes the current stage into `ProjectStageChangeSelect`, and on selection writes these exact changes: set `stage` to the selected stage id, set `progressPercent` to `getProjectProgress(nextStage)`, prepend one history entry object with the keys `stage`, `changedAt`, and `changedBy`, set `changedAt` to `new Date().toISOString()`, and set `changedBy` to the current responsible person's name. Compute `nextStageLabel` from `WORK_STAGE_ORDER` and render it beside the `Proxima etapa` label. Do not mutate the shared `PROJECTS` export and do not add persistence, server actions, or URL state.</action>
  <verify>rg -n "Mudar estagio|WORK_STAGE_ORDER|Select" apps/web/components/platform/projects/project-stage-change-select.tsx && rg -n "getProjectProgress|new Date\\(\\)\\.toISOString\\(\\)|progressPercent|changedAt|changedBy|nextStageLabel|setProject" apps/web/components/platform/projects/project-detail-page.tsx apps/web/lib/projects-data.ts</verify>
  <acceptance_criteria>
    - `apps/web/components/platform/projects/project-stage-change-select.tsx` contains the exact label `Mudar estagio`
    - `apps/web/components/platform/projects/project-stage-change-select.tsx` contains `WORK_STAGE_ORDER`
    - `apps/web/lib/projects-data.ts` contains `getProjectProgress`
    - `apps/web/components/platform/projects/project-detail-page.tsx` contains `new Date().toISOString()`
    - `apps/web/components/platform/projects/project-detail-page.tsx` contains the exact keys `changedAt` and `changedBy`
    - `apps/web/components/platform/projects/project-detail-page.tsx` contains `nextStageLabel`
  </acceptance_criteria>
  <done>The project detail view now supports a believable mock stage-change flow with immediate progress and history feedback.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm lint && pnpm typecheck && pnpm build`
- [ ] `rg -n "getProjectById|notFound" "apps/web/app/(platform)/projetos/[projectId]/page.tsx"`
- [ ] `rg -n "getProjectById|Detalhe" apps/web/components/platform/app-breadcrumbs.tsx`
- [ ] `rg -n "Mudar estagio|Ver anteprojeto|Gerar relatorio|Progresso|Historico de etapas|Baixar arquivo" apps/web/components/platform/projects/project-detail-page.tsx`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The projetos flow has a real detail page with local stage changes, progress, attachments, and visible links back into the broader project pipeline
  </success_criteria>

<output>
After completion, create `.planning/phases/06-projetos-e-pipeline-de-obras/06-02-SUMMARY.md`
</output>
